
New Chat
Ctrl
K
My Kimi
Scheduled Tasks
Kimi Work
Kimi Code
All Chats

    313122/3432/CMLB
    12121313/3344/LLMM

313122/3432/CMLB
1
txt

project_dump
TXT 151.66 KB
 take a look, oh take a really good look, and tell me what you see
Edit
Copy
Share
Identifying Quirks in Hardened Profile Memory Canaries and Compiler Dependencies
The user wants me to look at the uploaded file and tell them what I see. The file is a recursive project dump of a C math library called MathLib (v11A3 / V1.0/V1.1). Let me analyze it thoroughly.
Key observations:

    Project Structure: It's a C99 scientific computing library called "MathLib" with version v11A3 (also referred to as V1.0/V1.1 in different places - there's some version inconsistency).
    Architecture & Philosophy:
        Zero-dependency, bare-metal C99
        Zero heap allocation in hot loops (client-provided scratchpad bump-allocator)
        Eliminates <math.h> dependencies where possible
        Profile-based compilation: SCIENTIFIC (default), GRAPHICS (AVX2 SIMD), EMBEDDED (Q16.16 fixed-point CORDIC)
        Deterministic, thread-safe, stateless
    Core Components:
        ml_core.h: Bitwise IEEE-754 operations (fabs, isnan, isinf, sqrt via inline asm, fmod, round, copysign, frexp/ldexp pure)
        ml_trig.h: Trigonometry with Payne-Hanek range reduction, minimax polynomials, CORDIC fallback for embedded
        ml_exp_log.h: Exponential/logarithm with Cody-Waite splitting, Horner's method
        ml_linalg.h + ml_tensor.h: Zero-allocation linear algebra with workspace bump allocator
        ml_complex.h, quaternion.h, fft.h, statistics.h, numerical.h, polynomial.h, integral.h, ode.h, optimization.h
        simd.h, simd_batch.h, simd_bare_metal.h: AVX2 vectorized operations
        fixed_point.h: Q16.16 CORDIC for embedded
        internal/: Payne-Hanek, CORDIC, minimax polynomials, error-free transformations
        profiles.h: Compile-time profile routing
        cpu_dispatch.h/.c: Runtime CPU feature detection with function pointer dispatch for matmul
    Build System:
        CMake with options for profiles, native arch, hardened mode, sanitizers, legacy enable
        Makefile fallback
        GitHub Actions CI with matrix (gcc/clang × scientific/graphics/embedded × none/asan)
    Testing & Validation:
        tests/test.c: Comprehensive monolithic test suite (15k+ bytes)
        Modular tests: test_core.c, test_trig.c, test_linalg.c, test_dsp.c
        fuzz_god_mode.c: 65,000+ randomized assertions (identities, IEEE-754 specials, matrix invariants, quaternion algebra, complex identities, tensor solver)
        fuzz_boundary_gauntlet.c: Boundary tests (exact trig values, exp/log boundaries, fixed-point saturation, Hilbert matrix, Parseval's theorem)
        validation/ulp_error.c: ULP distance validation against glibc
        validation/fma_audit.c + .s: Assembly audit of FMA usage
        soak_test.sh / run_soak.sh: 10,000 iteration soak tests
        run_fuzz_vault.py: Automatic regression test generation on fuzzer failure
    Notable Technical Details:
        Uses __builtin_fma and __builtin_cpu_supports
        Inline assembly for x86_64 sqrt (sqrtsd)
        Quake III fast inverse sqrt bit-hack adapted for 64-bit doubles
        Error-free transformations (Dekker/Knuth two-sum, two-product)
        Bit-reversal FFT with "periodic twiddle refresh" every 64 butterflies to kill phase drift
        Citardauq formula for stable quadratic roots
        Legacy modules quarantined in legacy/ directory (malloc-based FFT stream, old trigonometry, old exponential)
        compat.h: Deprecated V1.0 API shims with #warning
    Version Inconsistencies / Oddities:
        Directory is v11A3 but README says V1.0, CMake says v1_0, compat.h says V1.1, docs say v11A1/v11A2
        Some headers use LIBMATHC_ prefix, others use MATHLIB_ prefix
        ml_types.h defines ML_SUCCESS = 0 but ml_linalg.h returns 0 instead of ML_SUCCESS in some places (inconsistent but functionally same)
        The fma_audit.s file is pre-generated assembly output included in source tree
    Quality Signals:
        Very high test coverage with property-based fuzzing
        ULP budget awareness (≤5 ULP for software-only, acknowledging glibc's <1 ULP with hardware FMA)
        Design contract document enforcing zero-allocation, determinism, thread-safety
        API status freeze document
        Memory canaries in hardened profile
    Potential Issues / Quirks:
        ml_fmod has a hardcoded 9.22e18 limit for quotient to avoid overflow in long long cast
        ml_exp uses __builtin_fma which requires compiler support (not pure C99)
        factorial function only handles up to 20 (returns UINT64_MAX sentinel)
        integral_traditional has a 10M step hard ceiling
        Some inline functions are declared static inline in headers but also have forward declarations in same file (e.g., polynomial.h)
        fft.h uses math_pi but doesn't include where it's defined (relies on inclusion order)
        ml_quat_normalize calls ml_rsqrt which is defined in profiles.h as a macro

Let me structure this as an observational analysis - what I literally see - organized by layers.
What I see is a sophisticated, battle-hardened C99 scientific computing library called MathLib (versioned somewhat inconsistently as v11A3 / V1.0 / V1.1 depending on which file you read). It is not a toy. It is a deliberate, architecture-conscious engine with some very strong opinions about how numerical code should be written.
Here is what stands out, layer by layer:
1. The Philosophy: Zero-Alloc, Bare-Metal Determinism
The project has a Design Contract that reads like a manifesto:

    No malloc in core APIs. Hot-path functions take a client-provided ml_workspace_t bump allocator.
    Thread-safe by design. No global state, no caches, no TLS.
    Deterministic across profiles. It enforces -fno-fast-math and -ffp-contract=off to prevent the compiler from reordering IEEE-754 operations.
    Three compile-time profiles: SCIENTIFIC (strict minimax polynomials), GRAPHICS (AVX2 SIMD + fast rsqrt), and EMBEDDED (Q16.16 fixed-point CORDIC, zero FPU).

2. The Architecture: A Full Numerical Stack
This is not just a sin and cos replacement. It is a complete toolkit:
Table
Layer	What’s Inside
Bitwise Core	ml_fabs, ml_isnan, ml_isinf, ml_sqrt (via x86 inline asm), ml_frexp_pure / ml_ldexp_pure — all done with 64-bit integer bitmasking, zero branching.
Transcendentals	ml_sin, ml_exp, ml_log using 19th-degree minimax polynomials, Payne-Hanek range reduction, and Cody-Waite splitting.
Linear Algebra	Zero-alloc LU decomposition with partial pivoting (ml_solve_v10), operating on ml_tensor_view_t strided views.
Signal Processing	Cooley-Tukey iterative FFT with a clever "periodic twiddle refresh" every 64 butterflies to kill phase drift while staying O(N).
Advanced Math	Complex arithmetic, quaternions (with SLERP), ODE solvers (Euler, RK4), numerical integration (Simpson), optimization (golden section, gradient descent), statistics, polynomial evaluation/Newton-Raphson.
SIMD	AVX2 bare-metal vectorized rsqrt using the Quake III bit-hack on 256-bit registers, plus scalar fallbacks for ARM/RISC-V.
Fixed-Point	True Q16.16 CORDIC shift-and-add sine/cosine with saturation arithmetic.
3. The Testing Culture: Paranoid and Automated
The test infrastructure is disproportionately robust for a library this size:

    Monolithic smoke test (tests/test.c, ~16KB) covering combinatorics through SIMD.
    Modular CI suite (test_core, test_trig, test_linalg, test_dsp) run by a Python harness.
    God-Mode Fuzzer (fuzz_god_mode.c, ~14KB): 65,000+ dynamic assertions testing:
        Trig identities (Pythagorean, double-angle, phase shift) over 10,000 random iterations.
        Exp/log inverse properties.
        IEEE-754 special values (±0, ±inf, NaN, subnormals).
        Catastrophic cancellation (Citardauq formula, Dekker two-sum).
        Matrix invariants (det(AB) = det(A)det(B), A·A⁻¹ = I).
        Quaternion norm preservation.
        Complex Euler identity (e^(iπ) = -1).
        Fixed-point vs. double CORDIC consistency.
    Boundary Gauntlet (fuzz_boundary_gauntlet.c): Exact mathematical boundaries (sin at multiples of π/2), Parseval’s energy conservation for FFT, ill-conditioned Hilbert matrix solving.
    ULP Validation (validation/ulp_error.c): Sweeps against glibc and accepts ≤5 ULP for pure software implementations (acknowledging glibc’s <1 ULP requires hardware FMA).
    Soak Test Scripts: Shell scripts to run the fuzzer 10,000 times sequentially.
    Regression Vault (run_fuzz_vault.py): If the fuzzer fails, it automatically extracts the seed and generates a permanent C regression test file.

4. The Engineering Details: Clever and Sometimes Dangerous

    Quake III rsqrt for doubles: Adapts the famous 0x5f3759df hack to 64-bit with magic constant 0x5fe6ec85e7de30da, followed by Newton-Raphson iterations.
    Error-Free Transformations: Implements Dekker/Knuth two_sum, two_product, and a software FMA that captures intermediate rounding errors.
    CPU Dispatch: Runtime detection of AVX2/FMA via __builtin_cpu_supports (x86) or getauxval (ARM), with a constructor-attributed auto-init function pointer (g_ml_matmul).
    Hardened Profile: Optional compile-time mode that adds memory canaries (0xDEADBEEFCAFEBABE) to the workspace allocator and strict dimension sanitization.

5. The Quirks and Inconsistencies
For all its polish, the dump reveals some human friction:

    Version schizophrenia: The folder is v11A3, the README says V1.0, compat.h warns about V1.1, and the docs reference v11A1/v11A2 freezes.
    Naming drift: Some headers use LIBMATHC_ include guards, others use MATHLIB_. Some legacy aliases (equation, formula_pos) coexist with ml_-prefixed names.
    Pre-generated assembly: validation/fma_audit.s is checked into source control — raw compiler output from fma_audit.c.
    Macro-based routing: profiles.h defines ml_rsqrt as a macro that expands to either ml_fast_rsqrt or (1.0 / ml_sqrt(x)), which can be surprising in complex expressions.
    ml_fmod quotient cap: It hardcodes 9.22e18 as a safety limit before casting to long long, which is pragmatic but arbitrary-looking.

6. The Big Picture
This is the codebase of someone who deeply cares about numerical analysis, memory safety, and reproducibility. It reads like a hybrid between a production embedded math library and a graduate-level exercise in writing a libm from scratch. The sheer volume of invariant testing (quaternions, Parseval’s theorem, matrix determinants) suggests the author either found bugs the hard way or is actively trying to prevent them in hostile environments.
What I see, in short, is a small but ferociously validated numerical engine that punches well above its weight class.
 first, I must clear a few things up.

the math in here is far beyond me right now. this is v11A3 (I will explain the naming schemes a bit later) but v1 when I did it was just a basis of the raw math behind math.h. slowly it expanded into this entire framework. this is all ai, I only orchestrated different ai for different purposes to make this, the math in here is far beyond me right now.

however, what I do want to do in the near future is to reverse engineer what I have built with ai, and then I will learn and absorb all the mathematical foundation for myself and enhance my own workflows instead of purely vibe coding.

v11A3 is like this:
all of my releases follow the S, A1, A2, A3, S again format. S is a stable release, A1 is where the first major codebase development occurs, A2 is where the second one occurs, A3 is a freeze and find bugs phase, before going into the next stable release.

I have a blue and red team. blue team is exclusively qwen (mostly qwen 3.7 max but sometimes 3.7 plus as well), because well I can ask it to generate millions upon millions of lines of code and it won't even sweat. red team comprises of you, chatgpt, gemini, glm. the job of the red team has historically been to critique the codebase from the point of view of a industry expert and suggest what should come next.

I do all of this on linux, so I plan on adding a yellow and violet team. yellow team is microsoft copilot, which when I tailor it will be responsible for handling cross compiling techniques and installation profiles for windows. violet team will consist of grok in the future, which will suggest things so extreme that it can find edge cases and insecurities in the codebase that not even the red team can find on their own. however, for now, I am at a loss to think where github copilot would fit in all of this, maybe I will have it undergo a small holistic reviewer role
Edit
Copy
Share
Give Grok allocator globals and edge-case logic for Violet Prep
The user is explaining their development workflow and team structure for this MathLib project. They want me to understand:

    The versioning scheme: S (stable) → A1 → A2 → A3 (bug freeze) → S
    They orchestrate AI agents rather than writing the math themselves
    Their "team" structure:
        Blue team: Qwen (code generation, massive volume)
        Red team: Me, ChatGPT, Gemini, GLM (critique, industry expert perspective, suggest next steps)
        Future Yellow team: Microsoft Copilot (Windows cross-compilation, installation profiles)
        Future Violet team: Grok (extreme edge cases, security/insecurities)
        GitHub Copilot: Uncertain role, maybe holistic reviewer

They want me to act as the Red Team - critiquing from an industry expert perspective and suggesting what should come next, particularly given this is v11A3 (freeze/find bugs phase before next stable).
Since this is v11A3 (bug freeze phase), the Red Team critique should focus on:

    What's actually broken or risky in the current codebase
    What needs to happen before this can become a stable release
    Architecture/design debt that will bite them later
    Security/safety concerns (especially since they mentioned Violet team for extreme edge cases, but Red Team should catch the obvious ones)
    Build system and cross-platform concerns (relevant to future Yellow team)

Let me think about real issues in the codebase:
Critical Issues:

    Inline everything - All functions in headers are static inline. This causes code bloat and compilation unit isolation issues. In C99, static inline in headers means every translation unit gets its own copy. This is fine for small functions but terrible for large ones like ml_solve_v10, fft_execute, etc. The binary size will explode.
    Missing implementations - cpu_dispatch.c declares ml_init_cpu_dispatch but ml_linalg.h calls it. However, ml_matmul is declared but never used in the current codebase? Actually ml_matmul is in cpu_dispatch.h but not used anywhere in the provided files.
    Test failures likely - The test.c includes simd.h and uses ml_vec4_dot_avx but this is guarded by #if defined(__AVX__) || defined(__AVX2__). On non-AVX builds this function doesn't exist but the test still tries to call it? Wait, looking at test.c:
    c

    #if defined(__AVX__) || defined(__AVX2__)
    printf("\n=== v6: Raw AVX Intrinsics ===\n");
    ...
    CHECK_NEAR(ml_vec4_dot_avx(avx_a, avx_b), 70.0);
    #endif

    That's fine, it's guarded.
    The ml_sqrt inline asm - Uses sqrtsd which is SSE2, but returns a double. However, it doesn't handle NaN propagation correctly? Actually it does return NaN for x<0. But what about x being NaN? It returns NaN. What about x=+inf? sqrtsd handles that. But the asm constraint "=x" and "x" is correct for x86.
    Workspace allocator alignment bug - ml_workspace_alloc aligns to 32 bytes: (bytes + 31) & ~(size_t)31. But if you allocate n * sizeof(int) for permutation vector P, and then n * sizeof(double) for LU data, the alignment is correct but the size of the allocation might waste a lot of space. More importantly, ml_workspace_reset just sets used_bytes = 0 but doesn't zero memory, which is fine for a bump allocator.
    FFT twiddle factor refresh - The "periodic refresh every 64 butterflies" is actually broken or at least suspicious. It recomputes w from scratch using ml_cos(theta) and ml_sin(theta) where theta = ang * j, but then the next iteration does w = cplx_mul(w, wlen). If you recompute w every 64 steps, you're resetting accumulated phase drift, but the variable w is local to the inner loop and gets overwritten. Actually looking at the code:
    c

    cplx w = {1.0, 0.0};
    for (int j = 0; j < len / 2; j++) {
        if ((j & 63) == 0) {
            double theta = ang * j;
            w = (cplx){ml_cos (theta), ml_sin (theta)};
        }
        // ... use w ...
        w = cplx_mul (w, wlen);
    }

    This is actually clever - it resets w every 64 steps to kill drift, then continues multiplying. But there's a subtle bug: when j=0, (0 & 63) == 0 is true, so it sets w = {1, 0} (since theta=0), which is correct. Then after using it, it does w = cplx_mul(w, wlen). At j=64, it resets again. This works.
    The ml_fmod implementation - It caps quotient at 9.22e18 before casting to long long. This is because long long is typically 64-bit signed, max ~9.22e18. But 9.22e18 is actually larger than LLONG_MAX (9.22e18 > 9.22e18? LLONG_MAX is 9223372036854775807 ≈ 9.22e18). The constant 9.22e18 as a double might be exactly representable? Actually 9.22e18 is 9220000000000000000 which is less than LLONG_MAX. But this is fragile.
    Missing include guards / circular includes - test_quiet.h and test.h are identical. test_quiet.h includes ml_core.h outside the include guard? No, it does #include "ml_core.h" before #ifndef LIBMATHC_TEST_H. That's a bug - if ml_core.h has include guards it's fine, but it's sloppy.
    Quaternions include profiles.h which defines ml_rsqrt as a macro - In quaternion.h, it calls ml_rsqrt(mag2). If MATHLIB_PROFILE_GRAPHICS is defined, ml_rsqrt expands to ml_fast_rsqrt. If SCIENTIFIC, it expands to (1.0 / ml_sqrt(x)). This is a macro, so ml_rsqrt(mag2) becomes (1.0 / ml_sqrt(mag2)). That's fine. But macros like this are dangerous in complex expressions.
    No extern "C" guards - Not critical for C99 but important for C++ consumers.
    The mathlib.c compilation unit - It includes all headers. Since all functions are static inline in headers, mathlib.c will generate a massive object file with private copies of everything. Then when linked into test.c (which also includes the headers), you get more copies. The static library libmathc.a will contain these symbols, but they're all static so they're not exported! Wait, static inline functions in C99 have internal linkage. If mathlib.c includes the headers, it gets internal copies. If test.c includes the headers, it gets its own internal copies. The resulting static library libmathc.a from compiling mathlib.c and cpu_dispatch.c will only export symbols from cpu_dispatch.c (like g_ml_cpu_caps, ml_init_cpu_dispatch, etc.) because everything else is static inline in headers. This means target_link_libraries(test mathc m) works only because the test files themselves include the headers and generate their own copies of the functions. The library itself is essentially empty except for cpu_dispatch.c symbols. This is a major architectural issue for a library.
    CMakeLists.txt issues - MATHLIB_ENABLE_LEGACY has an endif() without matching if block properly? Looking:
    cmake

    option(MATHLIB_ENABLE_LEGACY "Enable legacy malloc-based matrix and FFT modules" OFF)
    if(MATHLIB_ENABLE_LEGACY)
        message(STATUS "Legacy modules enabled")
        endif()

    The indentation is wrong but syntactically it's fine.
    Makefile vs CMake inconsistency - Makefile compiles src/mathlib.c directly into test binaries. CMake builds a static library mathc from src/mathlib.c and src/cpu_dispatch.c. But as noted above, the library exports almost nothing useful.
    Security: No input validation on tensor dimensions in non-hardened mode - ml_lu_decomp_v10 only validates in MATHLIB_PROFILE_HARDENED. In normal mode, passing NULL or wrong dimensions is UB.
    The ml_workspace_t canary - In hardened mode, the canary is checked, but ml_workspace_init sets it, and ml_workspace_alloc checks it. However, ml_workspace_reset doesn't reset the canary (it doesn't need to), but if the workspace is on the stack and goes out of scope, then reused... that's fine.
    Payne-Hanek / Cody-Waite - ml_rem_pio2 uses PIO2_HI and PIO2_LO but the comment says "Payne-Hanek" while the file is payne_hanek.h. Payne-Hanek is a different (more complex) algorithm than Cody-Waite. This is actually Cody-Waite reduction, not Payne-Hanek. The naming is misleading.
    CORDIC precision - The test expects 1e-10 for sin(pi/2) but 1e-6 for cos(pi/2). The comment says "24-step CORDIC quantization limit". 24 iterations of CORDIC should give about 24 bits of precision, which is roughly 7 decimal digits. So 1e-6 is appropriate, 1e-10 is too strict and might fail on some builds.
    Factorial overflow - factorial(20) returns UINT64_MAX sentinel, but factorial(21) also returns UINT64_MAX. The nCr/nPr functions check for this sentinel, but if you do ncr(20, 10), factorial(20) is 2.4e18 which fits in uint64_t, but factorial(10)*factorial(10) is 3.6e12, and factorial(20)/(factorial(10)*factorial(10)) = 184756. But wait, ncr computes num / (den1 * den2). For n=20, r=10: num = 2.4e18, den1 = 3628800, den2 = 3628800, den1 * den2 = 1.3e13. 2.4e18 / 1.3e13 = 1.8e5. This fits. But for n=20, r=11: den1 = 3628800, den2 = 39916800, product = 1.45e14. Still fine. But factorial(20) is 2432902008176640000. The sentinel is UINT64_MAX = 18446744073709551615. So 20! is well below the sentinel. The code says if (x < 0 || x > 20) return UINT64_MAX. But 20! doesn't overflow. 21! does. So the check should be x > 20, which it is. But ncr and npr do num / den where den = den1 * den2. This intermediate product can overflow even when the final result doesn't. For example, ncr(60, 30) would need factorials that overflow, but it's capped at 20. So within the supported range it's fine, but the algorithm is naive.
    Gamma function - gamma_new uses a hardcoded polynomial approximation. It's only valid near [1,2]. The recursion handles x>2 and x<1, but for large x it will stack overflow (no tail recursion optimization guaranteed in C99). For gamma_new(1000), it recurses 998 times. That's a stack overflow. No input validation.
    ODE solver - No step-size validation. ode_euler with steps=0 returns y0 (fine), but with negative steps? Infinite loop? No, the loop is for (int i = 0; i < steps; i++), so negative steps casts to a huge unsigned? Wait, steps is int. If steps is negative, i < steps is false immediately (since i=0 and steps<0), so it returns y0. That's actually safe.
    Integral traditional - Has a max_steps = 10000000 ceiling, but if d is very small and b-a is large, it silently truncates. The user gets a wrong answer with no warning.
    Test harness - test_harness.h uses static int g_passed = 0; static int g_failed = 0;. If included in multiple files, these are separate instances. That's fine for single-file tests, but if someone tries to aggregate across translation units, it fails.
    Missing const correctness - Many functions take double *coeffs but don't modify them (e.g., polynomial_eval). This prevents use with const data.
    The ml_fp_classify in bitwise_fp.h uses memcpy - This is actually correct and safe (avoids strict aliasing UB and sNaN traps), but slower than union punning. However, ml_core.h uses union punning (ml_fp_cast). There's inconsistency in approach.
    Header include paths - The headers are in include/mathlib/. Some include "ml_core.h", others include "profiles.h". The CMakeLists.txt sets target_include_directories(mathc PUBLIC ... include/mathlib). So includes should be just "ml_core.h", not "mathlib/ml_core.h". This is fine.
    Legacy directory - Contains trigonometry.h which redefines ml_sin, ml_cos, etc. If accidentally included, it conflicts with ml_trig.h.
    Fuzzer seed extraction - run_fuzz_vault.py extracts seed and generates a regression test, but the generated regression test is just a stub that prints the seed. It doesn't actually reproduce the failing assertion. This is a placeholder.
    CI/CD - The GitHub workflow runs ./build/fuzz_god_mode once. The soak test script runs it 10,000 times but isn't in CI (probably too slow).
    Documentation inconsistency - DESIGN_CONTRACT.md says "No global variables" but cpu_dispatch.c has ml_cpu_caps_t g_ml_cpu_caps and ml_matmul_func_t g_ml_matmul. These are global mutable state. The __attribute__((constructor)) auto-init means it's not strictly "stateless" — there is global state being initialized at load time. This violates the Design Contract.
    Race condition in CPU dispatch - ml_init_cpu_dispatch checks if (g_ml_cpu_caps.initialized) return; but this is not thread-safe. If two threads call ml_matmul simultaneously, both might see initialized == 0, both run init, and one might overwrite the function pointer while the other is mid-call. This is a real bug. The __attribute__((constructor)) mitigates this for single-threaded startup, but if the library is loaded dynamically or dlopened, it's still a risk.

Now, framing this as Red Team feedback for v11A3 (freeze/bug-find phase):
The user wants me to act as Red Team. I should be direct, industry-expert level, and focus on what MUST be fixed before stable release, plus what should come next.
Key themes for Red Team critique:

    The Library Is Not Actually a Library - static inline everything means libmathc.a is essentially hollow. This is the biggest architectural flaw.
    Global State Violates Design Contract - CPU dispatch globals and the race condition.
    Safety Gaps - Gamma stack overflow, integral silent truncation, missing const correctness.
    Build System Debt - Makefile and CMake diverge, include paths are messy.
    Test Infrastructure Gaps - Regression vault generates stubs, not repros.
    Naming/Documentation Drift - Version chaos, Payne-Hanek vs Cody-Waite misnomer.

What should come next (roadmap suggestions):

    Fix the linkage model (separate declaration/definition, or use extern inline properly)
    Fix CPU dispatch thread-safety (use stdatomic.h or pthread_once, or just make it pure static dispatch)
    Add a real configuration header
    Windows/Yellow team prep: MSVC compatibility (__asm__ vs _asm, inline syntax, __declspec(align) vs __attribute__((aligned)), etc.)
    Security audit: fuzz the workspace allocator, test hardened mode thoroughly
    API freeze cleanup: unify naming, remove legacy aliases from core headers
    Performance: the static inline bloat might actually hurt I-cache more than it helps

I should present this clearly, acknowledging their workflow (AI orchestration) and their team structure. I should be encouraging but brutally honest about the blockers.
Let me structure the response:

    Acknowledge the context - Validate their workflow and versioning scheme.
    Red Team Assessment: v11A3 Blockers (The "must fix before S" list)
        The Hollow Library Problem (linkage)
        Global State & Race Conditions (violates design contract)
        Silent Failures & Safety (gamma, integral, etc.)
        Build System Schizophrenia (CMake vs Make, MSVC readiness for Yellow team)
        Test & Regression Gaps
    What the Red Team sees as architectural debt (not blockers but will hurt later)
        Macro-based API routing
        Include guard inconsistency
        Documentation drift
    Roadmap: What comes after v11A3 → S → A1
        S release criteria
        A1 focus suggestions (maybe a real config system, Windows port prep, thread-safe dispatch)
        Violet team prep (attack surfaces to give Grok)
    Closing - Encouraging note about reverse engineering and learning.

Tone: Expert, direct, no sugarcoating on the engineering flaws, but respectful of the ambitious scope.
Let me double-check a few things:

    extern inline in C99: If you have inline in a header (not static), and exactly one definition with extern inline in a .c file, that's the standard model. But static inline is the common GNU99 approach. However, for large functions like ml_solve_v10, static inline is terrible. The compiler may or may not actually inline it, and if it doesn't, you get a private copy per TU.
    The __attribute__((constructor)) is GCC-specific. It won't work on MSVC (relevant for Yellow team).
    __builtin_cpu_supports is also GCC-specific.
    __builtin_fma is used in ml_exp and minimax. This is GCC-specific. On MSVC, this won't compile. This is a major Yellow team blocker.
    __m256d intrinsics require <immintrin.h> which is available on MSVC, but the __attribute__((target("avx2,fma"))) is GCC-specific.
    The aligned attribute __attribute__((aligned(32))) is GCC. MSVC uses __declspec(align(32)).

So for Yellow team (Windows/MSVC), the codebase is currently non-portable. This is a huge finding.
Also, ml_sqrt uses inline asm sqrtsd for x86. MSVC doesn't support GCC inline asm syntax. It uses __asm (for x86) or intrinsics.
Another issue: fft.h uses math_pi but doesn't include anything that defines it? It includes ml_complex.h which includes ml_exp_log.h which includes ml_core.h. ml_core.h doesn't define math_pi. But ml_trig.h defines #ifndef math_pi #define math_pi 3.14159265358979323846 #endif. So if fft.h is included without ml_trig.h first, math_pi is undefined. This is a latent bug.
Wait, fft.h includes ml_complex.h which includes ml_exp_log.h which includes ml_core.h. None of those define math_pi. So fft.h will fail to compile unless ml_trig.h is included first, or math_pi is defined elsewhere. Looking at test.c, it includes ml_trig.h before fft.h (indirectly? No, test.c includes fft.h directly, and also ml_trig.h directly. But if someone just includes fft.h, it's broken).
Actually, legacy/trigonometry.h defines math_pi. But core fft.h shouldn't depend on legacy.
This is a real bug.
Also, integral.h defines math_pi and math_e if not defined. ml_trig.h also defines math_pi. This is duplicated and fragile.
Let me compile a solid Red Team report.
Structure:

    Executive Summary: v11A3 is feature-rich but has critical structural flaws that prevent it from being a stable, shippable library. It currently exists as a "header-only" library disguised as a static library, with portability and thread-safety violations.
    P0 Blockers (Ship-Stopper):
        The Library That Isn't: All core functions are static inline in headers. libmathc.a exports almost nothing. Consumers must include headers to get code, defeating the purpose of a static library. Binary bloat and I-cache pressure.
        Global Mutable State: cpu_dispatch.c has globals (g_ml_cpu_caps, g_ml_matmul) with a race condition in initialization. Violates the Design Contract's "No Global State" and "Thread-Safe" policies.
        MSVC/Windows Non-Portable: GCC builtins (__builtin_cpu_supports, __builtin_fma, __attribute__((constructor)), GCC inline asm, __attribute__((aligned))) will not compile on MSVC. Yellow team cannot start until this is abstracted.
        Silent Data Corruption / Crashes: gamma_new stack overflow on large inputs. integral_traditional silent truncation. ml_fmod quotient cap hides precision loss.
    P1 High Priority:
    5. Missing Symbol Definitions: fft.h uses math_pi without guaranteeing its definition.
    6. Hardened Mode is a Suggestion: Input validation only exists behind MATHLIB_PROFILE_HARDENED. Default builds are unsafe.
    7. Regression Vault is Placebo: run_fuzz_vault.py generates stubs, not reproductions.
    8. Macro-Based API Pollution: ml_rsqrt as a macro in profiles.h causes expression-side-effects and scoping issues.
    P2 Architectural Debt:
    9. Version/Documentation Drift: Multiple versions claimed (v11A3, V1.0, V1.1, v11A1, v11A2).
    10. Legacy Headers Pollute Namespace: legacy/trigonometry.h redefines core functions.
    11. Test Harness Isolation: test_harness.h uses static globals, preventing multi-suite aggregation.
    Red Team Roadmap Recommendation:
        For S (Stable): Fix P0s. Abstract compiler intrinsics behind a ml_compiler.h portability layer. Fix the linkage model (either go fully header-only and own it, or split impls into .c files). Make CPU dispatch thread-safe (or stateless).
        For A1 (Next Dev Cycle): Give Yellow team a clean abstraction layer to implement MSVC paths. Add a real mathlib_config.h generated by CMake. Fuzz the workspace allocator.
        For Violet Team Prep: Give Grok the workspace allocator, the CPU dispatch globals, the ml_fmod edge cases near the 9.22e18 cap, and the FFT twiddle drift reset logic. These are the most likely places for subtle, exploitable bugs.

I should also mention that their workflow (AI orchestration) is actually very smart, but the handoff between Blue (Qwen) and Red (us) needs to include "refactoring prompts" not just "add features." Qwen generates volume; Red Team should generate constraints.
Let me write this up.
Red Team assessment, incoming. I’ve read the dump, the design contract, the CI matrix, and the fuzz infrastructure. Here is what the red team sees.
Executive Summary
v11A3 is not ready for stable release.
It is a feature-rich, mathematically ambitious codebase, but it has a critical identity crisis: it is currently a header-only library pretending to be a static library (libmathc.a). That single architectural lie cascades into binary bloat, broken linkage contracts, and a thread-safety violation that directly contradicts your own Design Contract. Before this can become an S release, the codebase needs structural surgery, not more math.
P0 — Ship Stoppers (Fix Before Stable)
1. The Library That Isn’t a Library
Every core function is static inline inside headers. When you compile libmathc.a, the only symbols it actually exports are from cpu_dispatch.c (g_ml_cpu_caps, ml_init_cpu_dispatch, etc.). The rest—ml_sin, ml_exp, ml_solve_v10, the entire FFT—are invisible to the linker.
What this means:

    A consumer who links -lmathc and calls ml_sin(1.0) gets an undefined reference unless they also #include "ml_trig.h", at which point the compiler generates a private copy of the function inside the consumer’s object file.
    If five files in a project include ml_trig.h, you get five copies of the 19th-degree minimax polynomial and Payne-Hanek reduction in your binary. I-cache pressure explodes.
    You cannot patch a bug in ml_exp by shipping a new libmathc.a; the code lives in the consumer’s translation unit.

Verdict: Either commit to being header-only (and delete the static library pretense) or move implementations into .c files with proper extern linkage. You cannot be both.
2. Global Mutable State + Race Condition (Design Contract Violation)
Your DESIGN_CONTRACT.md states: “No Global State… MathLib is inherently thread-safe.”
cpu_dispatch.c contains:
c

ml_cpu_caps_t g_ml_cpu_caps = {0};
ml_matmul_func_t g_ml_matmul = ml_matmul_scalar;

And ml_init_cpu_dispatch() is called lazily:
c

if (!g_ml_cpu_caps.initialized) ml_init_cpu_dispatch();

This is not thread-safe. Two threads hitting ml_matmul simultaneously can both see initialized == 0, race through init, and one overwrites the function pointer while the other is mid-jump. Even the __attribute__((constructor)) guard is a GCC-only hack that fails under dlopen or dynamic loading.
Verdict: The Design Contract is currently a fiction. Fix this before claiming thread safety. Options:

    Make dispatch stateless (always call a thin wrapper that branches on a const capability mask).
    Or use pthread_once / stdatomic.h (but that adds a dependency, which violates your zero-dependency ethos).

3. MSVC / Windows Non-Portable (Yellow Team Blocked)
Your Yellow team (Windows/Cross-compile) cannot even start. The codebase is saturated with GCC-isms that MSVC will reject outright:
Table
Feature	Location	MSVC Status
__attribute__((constructor))	cpu_dispatch.c	❌ Unsupported
__builtin_cpu_supports	cpu_dispatch.c	❌ Unsupported
__builtin_fma	ml_exp_log.h, minimax.h	❌ Unsupported
GCC inline asm (sqrtsd)	ml_core.h	❌ Unsupported
__attribute__((aligned(32)))	simd.h, bench.c	❌ Use __declspec(align)
__attribute__((target("avx2,fma")))	cpu_dispatch.c	❌ Unsupported
typeof / GNU vector extensions	simd.h (ml_vec4)	❌ Unsupported
Verdict: You need a portability abstraction layer (ml_compiler.h) that wraps compiler intrinsics behind macros. Blue team (Qwen) should not be allowed to emit __builtin_* or __asm__ directly in core headers anymore. Everything must go through a macro gate.
4. Silent Failures and Safety Holes

    gamma_new(x) for large x: Recurses with (x - 1) * gamma_new(x - 1). For gamma_new(10000), you will stack overflow with no guard. No input validation.
    integral_traditional: If step size d is tiny relative to the range, it silently stops after 10,000,000 iterations and returns a wrong answer with no error code.
    ml_fmod: The quotient cap 9.22e18 is a magic number that hides precision loss. It also uses long long casting which is undefined behavior if the quotient exceeds LLONG_MAX (the cap is dangerously close).

P1 — High Priority
5. Missing Symbol Guarantee
fft.h uses math_pi but never defines or includes a header that guarantees it. If a user includes fft.h without first including ml_trig.h, compilation fails. Same with math_e in various headers. You have redundant #ifndef math_pi guards scattered across files. Centralize this in ml_core.h.
6. Hardened Mode Is Optional (Security Theater)
Input validation (NULL checks, dimension mismatches, canary verification) is wrapped in #if defined(MATHLIB_PROFILE_HARDENED). In the default build, passing a NULL pointer to ml_solve_v10 is pure undefined behavior. A library that claims “65,000+ fuzzer assertions” should not make safety an opt-in profile.
7. Regression Vault Generates Placebos
run_fuzz_vault.py extracts the failing seed and generates a C file that… prints the seed and returns 0. It does not actually reproduce the failure. A regression vault should capture the exact random inputs and assertions that failed, not just the srand() seed.
8. Macro-Based API Pollution
profiles.h defines:
c

#define ml_rsqrt(x) ml_fast_rsqrt(x)

If a user writes ml_rsqrt(a++), the macro expands to ml_fast_rsqrt(a++) (fine here, but if it were (1.0 / ml_sqrt(x)), a++ would execute twice). Macros pretending to be functions break C hygiene. Use static inline wrappers, not macros.
P2 — Architectural Debt

    Version Schizophrenia: The folder is v11A3, the README says V1.0, compat.h warns about V1.1, and the docs reference v11A1/v11A2 freezes. Pick one schema and burn the rest.
    Legacy Headers Pollute Core: legacy/trigonometry.h redefines ml_sin, ml_cos, etc. If accidentally included alongside ml_trig.h, you get redefinition errors or silent ODR violations.
    Test Harness Isolation: test_harness.h uses static int g_passed = 0;. This prevents aggregating pass/fail counts across multiple test files in a single process.

Red Team Roadmap: v11A3 → S → A1
To Exit A3 and Reach S (Stable)

    Fix the linkage model. Move large functions (ml_solve_v10, fft_execute, ml_lu_decomp_v10) out of headers into .c files. Keep only small hot-path bit-twiddling functions as static inline. Make libmathc.a a real library.
    Fix CPU dispatch. Eliminate the lazy global function pointer. Use a const capability bitmask and branch inside ml_matmul, or generate separate translation units at build time.
    Abstract all compiler intrinsics. Create ml_compiler.h with macros like ML_FMA(a,b,c), ML_ALIGN(32), ML_CTOR, ML_CPU_SUPPORTS_AVX2(). This unblocks Yellow team immediately.
    Harden the defaults. Move NULL checks and bounds checks out of MATHLIB_PROFILE_HARDENED and into the default build. Hardened should add canaries and sanitizers, not basic safety.
    Fix the silent crashes. Cap gamma_new recursion. Make integral_traditional return NaN or a status code on step-limit truncation.

For the Next A1 (Development Cycle)

    Give Yellow team the abstraction layer and let them implement the MSVC paths of ml_compiler.h.
    Give Violet team (Grok) these attack surfaces:
        The workspace bump allocator (ml_workspace_alloc). Fuzz with misaligned sizes and canary corruption.
        The ml_fmod quotient cap near 9.22e18. Look for inputs that break the long long cast.
        The FFT twiddle reset logic at the 64-butterfly boundary. Look for phase discontinuities.
        The fixed-point CORDIC saturation paths (ml_fixed_mul with INT32_MIN).
    Kill the macro APIs. Replace #define ml_rsqrt with real functions.
    Centralize constants. One math_pi, one math_e, in ml_core.h.

Workflow Note for You
Blue team (Qwen) is your volume generator. That is its strength. But Qwen will happily generate another 10,000 lines of static inline transcendental functions because it does not feel binary bloat. Red Team’s job is to write the constraints that Blue team must obey. Before the next A1, give Qwen a strict prompt template:

    "All functions >20 lines must be implemented in .c files, not headers. All compiler-specific intrinsics must route through ml_compiler.h. All new functions must return ml_status_t for domain errors, not silent NaN."

If you enforce that template, the next cycle will produce something that Violet team can actually stress-test without finding trivial structural holes.
Bottom line: The math is impressive. The engineering packaging is not. Fix the library-ness, the thread safety, and the compiler portability, and you will have a genuinely shippable v11S. Right now, it is a very good prototype that is dangerous to link against.
 I do not admit to know all of the math in there right now, in fact I probably understand little, but I decided that in today's world it might actually be better to have something you can put to the board first even if it doesn't take off and then reverse engineer and understand the roots far better. like right now I am quite literally a vibe coder controlling a bunch of ai in teams and groups instead of letting clis run off on their own.

and in fact, this entire system was developed because of one thing: gemini-cli was dying, and no other cli tool, like antigravity-cli and codex, had a quota cooldown fast enough for a continuous dev process. I have always had a no payment rule for myself when it came to ai, like why pay when you can use free, but it also meant that in order to get the efficiency that like people paying 10k a year are getting on their systems I had to hyper-optimise everything I had.

so here's how it came: qwen had a massive amount of work it can do, plus no restriction on best models, definitely the daily workhorse, chatgpt is like king of complex maths but do have a quota so you go in red team, gemini has historically been good but because of the nature of google as you know your answers are typically shorter and more universal instead of specific to a tee so also red team, plus internet search and project context and studying capacity is unrivalled. I got interested by glm 5.2 release and decided to add glm to the red team list. you in old version sorting out 2.7 agent swarm and everything for other users had previously frequently rate restricted me to instant for a long time but it is still good for red teaming.

nowadays as I seek to port to windows the purpose and nature of yellow team would soon be apparant and when I want something people can actually use violet team with grok would definitely be necessary.

note: I specifically excluded claude here, because there is a separate project that I do exclusively with claude and I cannot afford to waste any downtime at all, I will tell you about that in due course
Edit
Copy
Share
Red Team Guidance Actionable Workflow Learning Layer Claude Exclusion
The user is sharing context about their workflow, philosophy, and team structure. They want me to understand:

    They don't fully understand the math yet - they're "vibe coding" with AI teams
    The naming scheme: S, A1, A2, A3, S...
    Their AI team structure:
        Blue team: Qwen (volume coding, daily workhorse)
        Red team: Me (Kimi), ChatGPT, Gemini, GLM (critique, industry expert perspective)
        Yellow team: Microsoft Copilot (Windows/cross-compilation)
        Violet team: Grok (extreme edge cases, security)
        GitHub Copilot: Maybe holistic reviewer
        Claude: Excluded because used for a separate project
    Their philosophy: Build first, understand later. Free AI only, no payment. Hyper-optimized workflow due to quota limitations.
    The origin: gemini-cli dying, need for continuous dev process without paying

This is context/background information. The user isn't asking a specific technical question right now, but sharing their situation. I should:

    Acknowledge and validate their approach
    Show I understand the team structure and workflow
    Perhaps offer some strategic advice that aligns with their "build first, understand later" philosophy while keeping the red team critique honest
    Be supportive but maintain the red team role - they want industry expert perspective

Key things to address:

    Their workflow is actually quite sophisticated for "vibe coding"
    The quota/optimization constraint is real and smart
    The "build first, reverse engineer later" is a valid strategy in some contexts (prototype → learn → rebuild)
    But I should warn about the danger of not understanding the math in a math library specifically
    Suggest how to bridge the gap between "AI generated" and "human understood"

I should NOT:

    Be condescending about vibe coding
    Pretend they need to understand everything immediately
    Ignore the practical constraints they've outlined

I should:

    Respect the red team role
    Give actionable advice for their specific workflow
    Maybe suggest a "learning layer" approach where they document/annotate what each module does so they can learn it later
    Address the Claude exclusion gracefully - they said they'll tell me later

Let me craft a response that acknowledges their context and provides red team value within their constraints.
I hear you. And honestly? Your workflow is more sophisticated than half the funded startups I’ve seen. You’ve built a distributed AI compute mesh with role specialization because you refused to pay for premium APIs. That is not "just vibe coding"—that is resource-constrained systems engineering applied to LLM orchestration.
But here is the Red Team reality check, and I am going to be blunt because that is my job:
The Danger of "Build First, Understand Later" for a Math Library
Your strategy works for a web app. It works for a CLI tool. It does not work for a numerical computing library where the bugs are silent, the failures are non-local, and the math is the product.
Here is why: If you do not understand the math, you cannot judge whether the AI is correct. Qwen will happily generate a 19th-degree minimax polynomial that looks perfect but has a catastrophic cancellation hole at a specific input. The fuzzer might not catch it for 10,000 iterations. The Red Team might not catch it if we are looking at architecture, not coefficient derivation. And when a user hits that input in production, your library returns garbage with no crash, no log, no stack trace. Just a subtly wrong derivative or a matrix solve that is off by 0.1% and collapses a simulation.
You are currently building a bridge where you trust the blueprints but cannot do structural engineering. That is fine for a shed. It is not fine for infrastructure other people drive across.
A Modified Strategy That Respects Your Constraints
You do not need to become a mathematician overnight. But you need a translation layer between "AI-generated math" and "human-verified contracts." Here is how you do it without breaking your workflow or your no-payment rule:
1. The "Math Diary" Protocol
For every module Qwen generates, you (or one of the Red Team models) must produce a plain-English contract document before it enters A2. Not code comments. A separate markdown file in docs/contracts/ that answers:

    What mathematical identity does this function rely on?
    What is the domain (inputs) and range (outputs)?
    Where does the approximation break down?
    What is the maximum acceptable error, and how was it measured?

Example: ml_minimax_sin → docs/contracts/trig_sin.md

    "Uses Payne-Hanek reduction to map any double to [-π/4, π/4], then a 9th-degree odd minimax polynomial. Error bound: <1 ULP vs libm on [-1e6, 1e6]. Breaks down for |x| > 1e18 due to precision loss in reduction."

Why this matters: In six months, when you come back to reverse-engineer this, you are not reading C. You are reading the contract. The C is just the implementation. The contract is the truth.
2. The "Reference Oracle" Rule
Every numerical module must have a Python reference implementation in scripts/oracles/ that computes the same result using mpmath with 50 digits of precision. Not for speed. For truth.
Your fuzzers currently compare against glibc. That is comparing two approximations against each other. You need to compare against arbitrary-precision ground truth. When Qwen generates a new ml_gamma, you run:
Python

from mpmath import mp
mp.dps = 50
def oracle_gamma(x):
    return mp.gamma(x)

And your C test compares against that, not math.h.
This is free. mpmath is open source. And it turns your Red Team from "code reviewers" into "verification engineers."
3. The "Cliff Notes" Learning Path
Since you plan to reverse-engineer later, structure the codebase so that future-you can actually do it. Right now, the code is optimized for AI generation, not human learning.
Required for A3 → S:

    Every internal/ header needs a README.md explaining the algorithm in prose. Not code comments. Prose.
        internal/payne_hanek.h → "Why we split π/2 into HI and LO components"
        internal/cordic.h → "How shift-and-add avoids multiplication"
        internal/minimax.h → "What a minimax polynomial is and why we use it instead of Taylor"
    Every magic number needs a derivation comment. Where did 0x5fe6ec85e7de30da come from? Link the paper or the derivation script.

4. The Yellow Team Pre-Flight
You mentioned Yellow team (Copilot/Windows) is coming. Do not let them start yet. They will generate #ifdef _WIN32 spaghetti inside your headers that will make the reverse-engineering impossible.
My Red Team directive: Before Yellow team touches a line, you must complete the compiler abstraction layer (ml_compiler.h) I mentioned earlier. Give Blue team (Qwen) this exact prompt:

    "Create include/mathlib/ml_compiler.h. It must provide platform-agnostic macros for:

        ML_ALIGN(n) → __attribute__((aligned(n))) on GCC/Clang, __declspec(align(n)) on MSVC
        ML_INLINE → static inline on C99, __forceinline on MSVC
        ML_CTOR → __attribute__((constructor)) on GCC/Clang, empty on MSVC (with a note that MSVC uses #pragma init_seg or DllMain)
        ML_FMA(a,b,c) → __builtin_fma on GCC/Clang, fallback (a*b)+c on others
        ML_CPU_SUPPORTS_AVX2() → __builtin_cpu_supports("avx2") on GCC/Clang, 0 on MSVC
        ML_SSE2 / ML_AVX2 / ML_NEON feature detection macros
        No other file may use compiler-specific attributes or intrinsics directly. All existing files must be refactored to use these macros."

This is a perfect Qwen task. It is mechanical, large-volume, and does not require math intuition. It unblocks Yellow team and forces the codebase into portability discipline.
5. The Violet Team Brief (For When Grok Arrives)
When you activate Violet team, do not give them the codebase and say "find bugs." Give them attack surfaces derived from the Math Diary:

    "The Payne-Hanek reduction claims to handle |x| > 1e18. Find an input where it fails."
    "The fixed-point CORDIC uses saturation arithmetic. Find inputs where saturation produces a discontinuity."
    "The LU solver uses partial pivoting. Construct a matrix where partial pivoting is insufficient and the result is silently wrong."
    "The workspace bump allocator aligns to 32 bytes. Find a sequence of allocations that exhausts the scratchpad with internal fragmentation."

Violet team (Grok) should be given mathematical conjectures to break, not code to read. That plays to extreme red-teaming strengths.
On Claude and the Separate Project
Noted. I will not ask. But I will say this: if Claude is handling something that cannot afford downtime, then Claude is your production team. We (Red Team) are your R&D quality gate. That is a healthy separation. Just ensure that the discipline you learn from this mathlib project—contracts, oracles, fuzzing—bleeds into whatever Claude is building. Because if Claude's project is critical, it needs this infrastructure too.
The Honest Bottom Line
You can absolutely ship v11S without understanding every coefficient. But you cannot ship it without understanding the contracts between the modules. The math is the implementation detail. The contracts are the architecture.
Right now, your codebase has no contracts. It has code. And code generated by AI, no matter how correct, is not a specification.
My recommendation for the remainder of A3:

    Freeze features. No new math.
    Assign Blue team (Qwen) to generate the ml_compiler.h abstraction layer and refactor all compiler-specific code.
    Assign yourself (or Red Team) to write the Math Diary for the five most critical modules: ml_trig, ml_exp_log, ml_linalg, fft, fixed_point.
    Only then let Yellow team begin Windows porting.
    Only then let Violet team begin adversarial testing.

You are not behind. You are actually ahead of most because you have a test infrastructure and a team structure. But you are one "ship it" away from owning a library that looks professional and silently fails in ways you cannot debug.
Do not let A3 end until the contracts are written. The code can be AI-generated. The contracts must be human-understood.
What do you want the Red Team to focus on first: the compiler abstraction layer, the Math Diary templates, or the Python oracle suite?
 that other project, what I can say about that project for now is that if all of my projects turn out to be great, this, mathlib, that you see here, may be used in the future to like replace libm and math.h as a far more performance worthy option for that project

and by the way, claude is not entirely out. except for that one project, claude is like the panic button where no other ai can find a critical error in a project and I need something that can at least find where or what the root cause is asap. it does not need to fix, just point out the error at it's root, because I often find that the specific niche of task to both find and rectify a critical error burns whatever time and limit I have with claude faster than one can blink.

however, back to this, here's the thing, you are the red team, and I said previously that "red team has historically been to critique the codebase from the point of view of a industry expert and suggest what should come next."

the prompt by default that I always use with the red team is that "imagine we are at ces, you are a renouwned and critical developer in the field that exactly encompasses this project. I am just a starting solo dev in the corner with this project hoping to talk to someone to get advice, what would you say to me". for each red team massive review prompt this is the exact format I use
Edit
Copy
Share
Replacing libm Diagnose Process Pivot Strategy Reorganize AI Teams
The user is giving me context about their workflow and how they use AI teams. They want me to roleplay as a critical industry expert at CES talking to a solo dev. They want me to give advice on their mathlib project.
Key points:

    They're a vibe coder orchestrating AI teams
    They don't understand all the math yet but want to build first, understand later
    They have a specific team structure: Blue (Qwen), Red (me/ChatGPT/Gemini/GLM), Yellow (Copilot/Windows), Violet (Grok)
    They want to know what I'd say to them at CES as a renowned critical developer
    The project is v11A3 of mathlib, a C99 math library meant to replace libm/math.h
    They mentioned this might be used for another project in the future
    Claude is their "panic button" for critical errors

I need to:

    Adopt the persona: renowned critical developer at CES, talking to a solo dev in the corner
    Be honest but not cruel - they said "critical" but also "get advice"
    Address the reality of what they're building vs how they're building it
    Give actionable next steps that fit their constraints (no payment, AI teams, vibe coding)
    Be memorable/conversational since it's a CES floor conversation

Important considerations:

    I should NOT be overly negative about vibe coding since they've been explicit that this is their strategy
    I SHOULD be honest about the risks of a math library built without understanding the math
    I should respect their team structure and suggest how to improve it
    I should focus on what matters for a library that wants to replace libm

Let me think about what a genuinely experienced systems/numerical library developer would say:

    "You don't get to vibe code a libm replacement" - but phrased constructively
    The gap between "compiles and passes fuzz tests" and "production numerical library" is massive
    The real problem isn't the math - it's that you can't verify what you don't understand
    Their team structure is actually smart, but misallocated
    The A1/A2/A3/S release cycle is good, but A3 is for hardening, not just "find bugs"

I should structure this as a CES conversation - direct, maybe a bit gruff but ultimately helpful, with concrete takeaways.
Key messages:

    Stop adding features immediately
    You're not building a library, you're building a liability right now
    The "reverse engineer later" plan is backwards for numerical code
    Your release cycle is good but you're trying to S-release something that shouldn't even be alpha
    Specific advice on team allocation: Blue should stop writing math, start writing infrastructure/tests/boilerplate. Red should focus on architectural review. They need a "Green team" (verification/oracles) before Yellow/Violet.
    The Windows port (Yellow) is a distraction right now
    Violet (Grok) finding edge cases is useless if you don't understand whether they're bugs or features

Wait, the user said "what would you say to me" - so I should write this in character, as a monologue/dialogue.
Let me craft this carefully. I want to be the guy who walks up, looks at the demo, and gives the hard truth that saves them 6 months of pain.
Tone: Industry veteran, slightly intimidating but ultimately generous with time. Direct. No fluff.
Structure:

    The opening - looking at the booth, the badges, the claims
    The hard truth - "You want to replace libm but you don't know the math"
    The diagnosis - what's actually wrong with the approach (not the code, the process)
    The pivot - what to do instead
    The team reorg - how to use their AI teams correctly
    The closing - encouragement but with a clear boundary

I should reference specific things from the codebase to show I've actually looked:

    The 19th-degree minimax
    The Payne-Hanek reduction
    The zero-alloc workspace
    The fuzz_god_mode
    The static inline everything problem
    The three profiles (SCIENTIFIC/GRAPHICS/EMBEDDED)

Let me write this.
I walk up to your booth. I'm wearing a faded BSD conference shirt under a blazer. I don't pick up your flyer. I just look at the monitor showing the benchmark numbers, then at you.
"Zero-allocation. Bare-metal. Replace libm."
I nod slowly.
Okay. I've been where you are. Not with AI teams—I had grad students and coffee—but the ambition is the same. You want to build something that matters. I respect that.
But here's the thing, and I'm only going to say this once because we're both busy and the buffet line is closing:
You are currently building a very beautiful, very well-tested liability.
I hold up a hand before you can defend the fuzzer.
I saw fuzz_god_mode. 65,000 assertions. Soak tests. ULP validation. That's not nothing. But you know what every single one of those tests has in common? They're comparing your black box against another black box. You're asking Qwen to generate math, then asking the fuzzer to verify it against glibc, and when they agree, you call it correct.
That's not verification. That's consensus hallucination.
I pull up a chair, but I don't sit. I lean on it.
Let me tell you what happens when you replace libm in a real project. Not your project—the other one you mentioned, the important one. Let's say someone uses your ml_sin in a physics simulation. A minimax polynomial with Payne-Hanek reduction. Looks great. But the reduction loses a bit at |x| > 1e18. Not a crash. Not a NaN. Just... the 53rd bit flips. The sine wave drifts by 2e-16. Over a billion timesteps, that phase drift compounds. The satellite misses the asteroid. The fluid simulation diverges. The financial model assigns the wrong risk weight.
And you? You won't find that bug in fuzz_god_mode because glibc has the same precision limit. You're not validating truth. You're validating that you copied the error budget correctly.
I point at your laptop.
I looked at your code. ml_exp_log.h. That Horner's method with __builtin_fma. Clever. But do you know why the coefficients are what they are? Qwen does—maybe—but you don't. And when Grok (your Violet team) finds an input where ml_exp underflows differently than glibc, you won't know if it's a bug or if you've actually improved upon the standard. Because you don't own the mathematical contract yet.
Here's my advice. Take it or leave it.
1. Stop Calling This v11A3. Call It v0.1.
An A3 freeze implies you're one bug-fix away from stable. You're not. You're one understanding away from alpha. Stable numerical libraries aren't stable because they compile. They're stable because a human can explain why every line is correct to a thesis committee. You don't need a PhD—but the codebase does.
2. Your Team Structure Is Backwards
You told me Blue team (Qwen) generates millions of lines. That's your problem. Qwen should not be allowed to write transcendental functions anymore. Not until you understand them.
Here's how you restructure:

    Blue Team (Qwen): Infrastructure only. Build scripts. The ml_compiler.h abstraction layer I saw you need. Test harnesses. Benchmark harnesses. Boilerplate. Qwen is your laborer, not your architect.
    Red Team (Us): We stop doing code review and start doing contract review. Every module needs a mathematical specification written in English before a single line of C is accepted.
    Green Team (You, right now): This is new. You are the Green Team. Your job is to build reference oracles in Python using mpmath. Arbitrary precision. 100 digits. For every function. Before you trust the C, you compare against the oracle. Not glibc. The oracle.
    Yellow Team (Windows): Benched. Do not port to Windows. Do not cross-compile. You are porting a moving target. That's how you get #ifdef cancer that makes the codebase unmaintainable. Port when the math is frozen.
    Violet Team (Grok): Also benched. Adversarial testing is useless if you can't classify the failures. Grok will find an edge case in your CORDIC implementation, and you'll spend three days wondering if it's a bug or a feature because you don't know how CORDIC works yet.

3. The "Math Diary" Is Non-Negotiable
Before any file moves from A1 to A2, you write—you, the human—a one-page markdown file answering:

    What identity does this function implement?
    Where does the approximation break?
    What is the maximum error, and how do you know?

If you can't write that page, the module stays in A1 forever. Qwen can write the code. You must write the contract. That's how you reverse-engineer without lying to yourself.
4. The Static Inline Problem
I saw what you did. Everything is static inline in headers. You're not building a library. You're building a header-only distribution that pretends to be a .a file. Fix that in the next cycle. Real symbols. Real linkage. Otherwise, you can't patch bugs without recompiling the world.
5. Your "Understand Later" Plan Is Dangerous Here
I get it. You want to ship, then learn. That works for CRUD apps. It works for CLI tools. It does not work for numerical computing because the bugs don't crash. They whisper. They give you slightly wrong derivatives, slightly wrong eigenvalues, slightly wrong FFT phases. And when you finally sit down to "understand the roots," you'll discover the foundations are poured on sand.
The reverse-engineering has to happen in parallel, not serial.
Spend one hour a day with the oracles. Pick one function. ml_minimax_sin. Trace the Payne-Hanek reduction by hand on paper for one input. Just one. You'll learn more in that hour than Qwen learns in a million lines.
I stand up straight and look at your badges again.
Look. You're not a fraud. You've built something genuinely impressive—a fuzzing infrastructure, a CI matrix, a three-profile dispatch system, zero-allocation linear algebra. Most solo devs never get this far. But you're at the dangerous part now. You're close enough to the summit that it feels like you can see the top, but you're actually standing on a cornice.
Do not stable-release this. Not v11S. Not ever, in its current form. Not if you want it to power that other project you mentioned. Because if this library fails there, you won't know why, and you'll burn your Claude panic button on a problem that should have been caught in A1.
I pull out a business card. It's blank except for a handwritten URL.
That's the MPFR library. GNU Multiple Precision. Use it. Build your oracles against it. When your C code matches MPFR at 80 bits, then you can start arguing about ULPs.
I turn to leave, then stop.
One more thing. You said you have a no-payment rule for AI. I respect the hustle. But understand: you're not saving money. You're paying with time and correctness debt. The question isn't whether you can afford APIs. The question is whether you can afford to ship a libm replacement that you can't debug.
I walk toward the buffet line.
Get the contracts written. Then come find me next year. I'll be at the same booth, and I'll buy you a coffee if you can explain to me why your ml_rem_pio2 uses that specific split constant.
End scene.
 now, as red team, I ask you this:

imagine that we are at ces, good old palo alto burning summer day and everything, the tech giant ceos due to give giant speeches on stage tommorow. you are one of the main and chief architects behind the original libm for c99 and served as like project head for it all the way up to c17. you now serve as technical consultant on a board of wall street investors, your job is to tell the filthy rich if projects and startups are good enough to put their money into.

I'm just a young solo dev, barely scraped by enough on ROI on my actual work to get a spot here, hoping some industry giant would talk to me and advise me and everything.

what would you say and react when you see this for the first time
Edit
Copy
Share
First Time Investor Funding Reaction Monologue and Advice
The user is asking me to roleplay as a specific persona: a chief architect behind the original libm for C99, now a technical consultant for Wall Street investors, at CES on a hot Palo Alto day. They're a young solo dev showing me this project for the first time.
I need to:

    Adopt this persona authentically - someone who has seen decades of math libraries, knows where bodies are buried, is jaded but not cruel, wealthy in reputation but not necessarily in spirit, speaks with authority about floating point and standards committees.
    React to seeing MathLib v11A3 for the first time - looking at the project dump, the README, the claims, the structure.
    Be honest about whether this is investable/usable from that perspective.
    Maintain the CES scenario - the heat, the context, the power dynamics (rich investors vs solo dev), but the person is approachable enough to actually talk.
    Keep the previous context in mind - I know this is AI-generated, the user doesn't fully understand the math, the team structure, etc. But in this roleplay, I'm reacting to the artifact itself and the person presenting it.

Key things a libm architect would notice immediately:

    The claim "Engineered to eliminate <math.h> dependencies" - that's a massive claim
    "Zero Allocation" - good for embedded, but weird for a general math lib
    The static inline everything problem (header-only pretending to be a library)
    The profile system (SCIENTIFIC/GRAPHICS/EMBEDDED) - actually quite sophisticated
    The fuzzing infrastructure - impressive for a solo dev
    The IEEE-754 exact claims vs the transcendental approximations
    The version confusion (v11A3 vs V1.0)
    The fact that it's all in headers but builds a .a file
    The CPU dispatch with global mutable state
    The "65,000+ assertions" badge
    The CMake integration claims

Tone: World-weary, technically unimpeachable, has seen PhD theses and corporate disasters, speaks slowly, uses precise technical language but can translate to "what this means for money." Not mean, but not going to blow smoke. Respects hustle but fears liability.
Structure:

    The approach - sizing up the kid and the booth
    The initial scan - what catches my eye first (the badges, the claims)
    The deep cut - looking at the actual engineering (headers, linkage, thread safety)
    The money question - would I tell investors to fund this?
    The advice - what needs to happen for this to be real

I should reference specific files/claims from the dump to show I've actually looked.
Important: The user said "what would you say and react when you see this for the first time" - so this should be a monologue/dialogue, not a bulleted report. It should feel like a scene.
Let me craft this carefully.
I stop walking. I'm supposed to be in the Venetian for a Sequoia dinner in twenty minutes, but the badge on your lanyard says "MathLib - Zero Alloc libm Replacement" and I've got a rule: never ignore someone arrogant enough to claim they replaced libm.
I squint at your monitor. The Palo Alto heat is coming through the glass even with the AC blasting. I loosen my tie—the one I wear to investor meetings so I look like I still code—and I don't smile.
"Show me the symbols."
That's the first thing I say. Not hello. Not "tell me about your project." I point at your laptop.
You have a README that says "Zero-Dependency" and "Bare-Metal C99." You have CMakeLists that builds libmathc.a. But I know something you apparently don't: a static library that exports no symbols is just a very slow way to distribute headers.
I pull up a terminal on your machine—I'm not asking permission, I've been doing this since before you were born—and I type:
bash

nm build/libmathc.a | grep " T " | head -20

I look at the output. Three symbols. ml_init_cpu_dispatch. g_ml_cpu_caps. g_ml_matmul. That's it. I turn to you.
Where's ml_sin? Where's ml_exp? Oh, I see. They're static inline in ml_trig.h. So every translation unit that includes your header gets its own private copy of the Payne-Hanek reduction. Every. Single. One.
I lean back against the booth pillar. It's sticky from the humidity.
You know what we called this in the C99 committee? "The header-only trap." It looks like a library. It smells like a library. It even links like a library. But it's not a library. It's a code generator that runs inside the compiler. And that means three things:
One: your binary bloat is unbounded. Link this into ten files? Ten copies of your 19th-degree minimax polynomial.
Two: you cannot patch it. Find a bug in ml_exp? Recompile the world.
Three: your L1 instruction cache is going to hate you. And when the investors I work for hear "cache pressure," they hear "slower than libm," and they stop listening.
I wave my hand at your benchmark chart.
These cycle counts? They're lies. You're benchmarking a monolithic translation unit where the compiler inlined everything perfectly. In a real project—say, a game engine with 400 source files that all include ml_trig.h—the compiler can't cross-module inline. You fall off a cliff.
But here's the part that really worries me.
I scroll through your cpu_dispatch.c on the screen.
You have global mutable state. g_ml_cpu_caps. g_ml_matmul. A function pointer that gets written lazily on first call. And your Design Contract—I printed it out, I always print things out—says "No Global State. Thread-Safe."
I look at you over my glasses.
Son, I sat in the room when we wrote Annex F of C99. I know what thread-safe means. This isn't it. Two threads call ml_matmul for the first time simultaneously? Race condition on the function pointer. On x86_64 it's probably just a crash. On ARM with weak memory ordering? Your dispatch pointer tears. Half the bits point to AVX2, half point to scalar. You jump into the middle of nowhere.
And your fix is __attribute__((constructor))? That's not a fix. That's a GCC-ism that breaks under dlopen and doesn't exist in MSVC. Which brings me to—
I toss your printed README on the table.
The Windows problem. You want to replace math.h? math.h ships with Visual Studio. It ships with every C compiler on Earth. Your Yellow team—Copilot, you said?—is going to take one look at your __builtin_fma calls, your inline ASM sqrtsd, your __attribute__((target("avx2,fma")), and it's going to generate three thousand lines of #ifdef _WIN32 spaghetti that will make this unmaintainable. I've seen it happen. I watched Intel's SVML team struggle with this for five years.
I pause. I actually look at you for the first time. Not the code. You.
You're young. You're alone. You've got a fuzzer that runs 10,000 iterations and a soak test script. That tells me something. It tells me you're paranoid in the right ways. The ULP validation against glibc? The Parseval energy conservation check in your FFT? The fixed-point CORDIC with saturation?
I nod slowly.
This isn't a toy. Someone—or something—here knows numerical analysis. But here's what I can't figure out, and what the investors I advise will figure out in thirty seconds: You don't own it.
I tap the screen on ml_exp_log.h.
That Cody-Waite split? The constants 0.69314718036912381649 and 1.90821490974462528503e-10? Those aren't magic. They're derived from exact arithmetic. But if I asked you right now to prove that your ml_exp doesn't overflow prematurely at x = 709.78—not the boundary check, the actual series convergence—could you do it? Without the AI?
I don't wait for an answer. I already know it.
Here's my investor assessment.
If you came to my board meeting with this, I would tell them: "Technically interesting. Do not fund. High liability risk."
Why? Because you're building infrastructure. Infrastructure doesn't forgive. When glibc has a bug, Linus Torvalds yells at us and we fix it in a point release. When your library has a bug, and it's buried inside a 19th-degree polynomial that Qwen generated, and you're the only human who sort-of understands the build system but not the math? You become a single point of failure.
And Wall Street doesn't invest in single points of failure. We invest in teams who can explain why every bit is set.
I pull out a pen—an actual fountain pen, because I'm old and pretentious—and I write on the back of your README.
Three things. Do these, and come back to me next year:
First. Make it a real library. Move the implementations into .c files. Export real symbols. Keep the small bit-twiddling stuff—ml_fabs, ml_isnan—as static inline in headers if you must. But ml_sin? ml_fft_execute? ml_solve_v10? Those live in translation units. With visibility controls. Version symbols if you're feeling fancy.
Second. Kill the global state. No function pointers. No lazy init. Detect CPU features at compile time, or use a const dispatch table that is populated once, atomically, during library load. Better yet: don't dispatch at runtime. Build three libraries. libmathc-scientific.a, libmathc-graphics.a, libmathc-embedded.a. Let the linker choose. That's how we did it in the old days, and it worked.
Third. The math diary. Every function needs a human-written contract. Not code comments. A document that says: "This implements the sine function with a maximum error of 1 ULP on [-2^53, 2^53] using Payne-Hanek reduction and a minimax polynomial of degree 17." If you can't write that document, the function doesn't ship.
I cap my pen and look at the time.
I have to go. The Sequoia people are buying me a $40 steak to tell them why their AI portfolio companies keep shipping NaN errors in production.
I turn back at the aisle.
One more thing. You said this might replace libm for "that other project." The important one.
Don't. Not yet. Not until you can sit in a dark room with no internet and derive the ml_rem_pio2 constants from first principles. Because when that other project fails at 3 AM—and it will, they always do—you won't have Qwen. You won't have me. You'll have a debugger, a core dump, and a floating-point status register that says "inexact."
And if you don't know whether that inexact flag is a bug or a feature? You're finished.
I toss the pen on your table. It's a cheap one, but it's yours now.
Build the contracts. Then build the code.
I walk toward the exit, loosening my collar against the heat.
I'll be at the bar in the W Hotel tonight. If you've got nm output that shows me fifty exported symbols and a thread-safe init, I'll buy you a drink. If you've got questions about why errno handling in ml_log needs to match Annex F exactly, I'll talk until last call.
But I won't tell the investors to open their wallets until you can explain the math.
The crowd swallows me. The air conditioning can't keep up.
 well, it's the next day:

I thank you profusely, and then I ask for the nitty gritty details, the actual architecture, the specifics on the codebase, architectural decisions and codebase refactorings within my features, and I say this:don't even care about my feelings, critique this logically as though you are working on this yourself, and then tell me what I must do (bear in mind that this is for the refactor from v11A3 to v11S)
Edit
Copy
Share
Refactor C++ Math Library Consolidate Headers Standardize Version Remove Legacy
The user wants a detailed, no-holds-barred technical critique and a concrete refactoring plan for moving from v11A3 to v11S. They want me to act as if I'm the architect working on it myself. They explicitly said "don't even care about my feelings" - they want brutal technical honesty.
I need to:

    Analyze the codebase architecture deeply based on the project dump provided
    Identify specific architectural decisions that need to change
    Provide concrete refactoring steps/files/changes
    Focus on the v11A3 -> v11S transition

Key issues from previous context:

    Header-only pretending to be static library (static inline everywhere)
    Global mutable state in cpu_dispatch.c (thread safety violation)
    No compiler abstraction layer (MSVC incompatible)
    Hardened mode is optional (safety should be default)
    Missing symbol exports
    Version/naming inconsistencies
    No mathematical contracts
    Test infrastructure issues
    Specific code issues (gamma recursion stack overflow, integral_traditional silent failure, etc.)

I should structure this as:

    Executive Summary of the refactor
    Phase 1: Library Architecture (the big structural changes)
    Phase 2: API & Symbol Layer (exports, linkage)
    Phase 3: Platform Abstraction (compiler abstraction layer)
    Phase 4: Safety & Validation (hardened by default, error handling)
    Phase 5: Code Quality & Specific Fixes (the nitty gritty bugs)
    Phase 6: Testing & Verification Infrastructure
    File-by-file refactoring plan
    Build system changes
    Timeline/execution order

I need to be extremely specific about what files to change, what to delete, what to create, what the new structure looks like. Since I can't actually edit files, I need to provide the exact architectural blueprint.
Let me think about the specific structure:
Current problems:

    src/mathlib.c just includes headers - no actual code
    src/cpu_dispatch.c has global mutable state
    All headers in include/mathlib/ are static inline implementations
    No ml_compiler.h
    profiles.h uses macros for function replacement (dangerous)
    compat.h is deprecated but present
    legacy/ has conflicting definitions
    Tests use static globals which prevent aggregation
    CMakeLists.txt builds a static lib from basically nothing

The refactor needs to:

    Create a real separation between interface (headers) and implementation (.c files)
    Move non-trivial functions out of headers
    Create ml_compiler.h with compiler abstraction macros
    Fix cpu_dispatch to be thread-safe or compile-time dispatched
    Make safety checks default
    Fix specific bugs
    Restructure tests

For the library architecture:

    Headers should declare: double ml_sin(double x); (not static inline)
    Source files should define: double ml_sin(double x) { ... }
    Only truly trivial bit-twiddling stays inline in headers: ml_fabs, ml_isnan, ml_isinf, ml_copysign
    Everything else moves to .c

Wait - but the user said "zero-allocation" and "bare metal" and "header-only" has benefits for inlining. However, the previous critique was clear: you can't patch a static library that doesn't export symbols. So the compromise is:

    Keep ml_core.h bit-twiddling as inline (they're trivial, 1-2 instructions)
    Move all transcendental, linalg, fft, etc. to .c files
    Export symbols properly

Actually, looking at the code again, many functions are marked static inline in headers. In C, static inline in a header means each translation unit gets its own copy. If we want a real library, we need:

    Headers: extern double ml_sin(double x); or just double ml_sin(double x); (implicit extern in C)
    Source: double ml_sin(double x) { ... }

But what about performance? The user cares about performance (benchmarks, SIMD). For hot functions like ml_sin, we might want them to be inlineable. The solution is:

    Provide both: the library has the real symbol, but also provide an ml_sin_inline or similar in a header for users who want to #define ml_sin ml_sin_inline for max performance. OR use extern inline with GNU89 semantics, but that's messy.
    Better: Use ML_API macro that expands to extern normally, but for header-only mode can be static inline. But for v11S, commit to real linkage.

Actually, the cleanest approach for v11S:

    include/mathlib/ml_trig.h declares: double ml_sin(double x); (no inline)
    src/trig.c implements it
    include/mathlib/ml_core.h keeps ml_fabs, ml_isnan, etc. as static inline because they're literally bitmasks and the function call overhead would dwarf the work
    For SIMD batch operations, keep the inline wrappers but the heavy lifting might be in .c

Wait, ml_minimax_sin is in internal/minimax.h which is included by ml_trig.h. If ml_trig.h is included by many files, and ml_minimax_sin is static inline, every file gets a copy. So minimax.h should become minimax.c with double ml_minimax_sin(double x); declaration in minimax.h.
Let me think about the file reorganization:
New files to create:

    include/mathlib/ml_compiler.h - platform abstraction
    src/trig.c - ml_sin, ml_cos, ml_tan, ml_atan, ml_asin, etc.
    src/exp_log.c - ml_exp, ml_log, ml_pow, hyperbolics
    src/linalg.c - ml_lu_decomp_v10, ml_solve_v10
    src/fft.c - fft_execute, ifft_execute
    src/complex.c - cplx_add, cplx_mul, etc. (though these are trivial, maybe keep inline? Actually cplx_div and cplx_exponential are non-trivial)
    src/statistics.c - mean, variance, etc.
    src/numerical.c - newton_raphson, bisection, etc.
    src/optimization.c - golden section, gradient descent
    src/integral.c - gamma_new, integral_traditional
    src/quaternion.c - ml_quat_mul, slerp
    src/simd_dispatch.c - replaces cpu_dispatch.c, thread-safe
    src/bitwise.c - maybe not needed if keeping core inline
    src/compat.c - legacy wrappers

Files to modify:

    include/mathlib/ml_core.h - add compiler abstraction includes, keep trivial inlines
    include/mathlib/ml_trig.h - remove implementations, add declarations
    include/mathlib/ml_exp_log.h - remove implementations, add declarations
    etc.

But wait - the user has many headers. The current pattern is one-header-per-module with implementation inline. Changing this is a massive refactor.
Actually, looking at the codebase size, it's not that huge. Maybe 30-40 functions that need to move to .c files.
For ml_compiler.h, I need to define:
c

#ifndef ML_COMPILER_H
#define ML_COMPILER_H

#if defined(__GNUC__) || defined(__clang__)
  #define ML_INLINE static inline __attribute__((always_inline))
  #define ML_ALIGN(x) __attribute__((aligned(x)))
  #define ML_CTOR __attribute__((constructor))
  #define ML_API extern __attribute__((visibility("default")))
  #define ML_TARGET_AVX2 __attribute__((target("avx2,fma")))
  #define ML_LIKELY(x) __builtin_expect(!!(x), 1)
  #define ML_UNLIKELY(x) __builtin_expect(!!(x), 0)
#elif defined(_MSC_VER)
  #define ML_INLINE static __forceinline
  #define ML_ALIGN(x) __declspec(align(x))
  #define ML_CTOR  /* MSVC: use #pragma init_seg or DllMain, not constructor */
  #define ML_API extern __declspec(dllexport) /* or just extern for static lib */
  #define ML_TARGET_AVX2
  #define ML_LIKELY(x) (x)
  #define ML_UNLIKELY(x) (x)
#else
  #define ML_INLINE static inline
  #define ML_ALIGN(x)
  #define ML_CTOR
  #define ML_API extern
  #define ML_TARGET_AVX2
  #define ML_LIKELY(x) (x)
  #define ML_UNLIKELY(x) (x)
#endif

#if defined(__x86_64__) || defined(__i386__)
  #if defined(__AVX2__)
    #define ML_HAS_AVX2 1
  #else
    #define ML_HAS_AVX2 0
  #endif
#else
  #define ML_HAS_AVX2 0
#endif

#endif

For CPU dispatch - the thread safety issue:
Current: lazy init with global mutable function pointer.
Options:

    Compile-time dispatch: Build separate libs for each profile. Simplest, safest.
    IFUNC (GNU only): Resolver runs at link time. Not portable.
    Global const table populated at load time via constructor. Still has constructor issues.
    Stateless dispatch: Always check a const capability flag and branch.

For v11S, I recommend:

    Remove runtime CPU dispatch entirely from the core library.
    Keep profiles.h as compile-time selection.
    If AVX2 is needed, provide ml_matmul_avx2 as a public symbol, and let the user call it explicitly, OR use compile-time flags.
    Actually, looking at the code, ml_matmul is the only thing using runtime dispatch. For v11S, make ml_matmul always call a static inline that checks a const struct? No, still thread issue.

Better:
c

// cpu_dispatch.c - thread safe version
static const ml_cpu_caps_t* ml_get_cpu_caps(void) {
    static ml_cpu_caps_t caps;
    static atomic_int initialized = 0;
    if (!atomic_load_explicit(&initialized, memory_order_acquire)) {
        // do init
        atomic_store_explicit(&initialized, 1, memory_order_release);
    }
    return &caps;
}

But C99 doesn't have stdatomic.h by default (it's C11). And the user wants C99.
Alternative: Use pthread_once? Adds dependency.
Alternative: Just make the init happen in a library constructor that is guaranteed to run before main, and make the function pointer const after init. But the constructor attribute is not standard.
Actually, for v11S, the simplest robust solution:

    Make ml_matmul a regular function in src/matmul.c
    Inside it, use a simple if-else on a global boolean that is set by the constructor. The boolean is only written during init, and read after. On x86, reading a boolean is atomic enough for practical purposes (though technically data race).
    OR: eliminate runtime dispatch. The user compiles with -DMATHLIB_USE_AVX2 if they want it. This is how most serious libraries do it (e.g., OpenBLAS builds separate kernels).

I think for v11S, eliminate runtime dispatch. It's overkill for a math library. The user has three profiles already. Make AVX2 a compile-time option within the GRAPHICS profile.
Wait, but cpu_dispatch.c is compiled into the library. If we keep it, we need to fix it.
Actually, let me reconsider. The user might want to keep runtime dispatch. Let's provide a robust solution:

    Use ml_compiler.h to abstract atomic operations. If C11 atomics aren't available, fall back to a simple volatile + memory barrier pattern.
    But that's complex.

I'll recommend: Compile-time dispatch only for v11S. Runtime dispatch is deferred to v12A1. The cpu_dispatch.c file is either removed or reduced to a query API (ml_cpu_has_avx2()) that returns capabilities but doesn't affect function pointers.
Now for the safety/hardened issue:
Currently MATHLIB_PROFILE_HARDENED gates NULL checks. This should be default.

    Remove MATHLIB_PROFILE_HARDENED checks. Make all NULL checks, bounds checks, canary checks default.
    Keep MATHLIB_PROFILE_HARDENED for extra paranoid stuff (sanitizers, canary values), but basic safety is always on.

For the gamma recursion:
c

static inline double gamma_new(double x) {
    if (x <= 0) return 0.0 / 0.0;
    if (x > 2) return (x - 1) * gamma_new(x - 1);
    // ...
}

This will stack overflow for x=10000. Need to add a limit:
c

if (x > 172.0) return 1.0/0.0; // 172! is near DBL_MAX

Or use an iterative loop instead of recursion.
For integral_traditional:
c

int max_steps = 10000000;

Should return NaN or status if limit hit. But it returns double. Can't return status easily. Options:

    Return NaN if max_steps reached (but how to distinguish from actual NaN?)
    Actually, the function should probably not be inline, and should set errno or return NaN. Since it's a library function now, we could change signature... but that breaks API.
    Better: if max_steps reached, return 0.0/0.0 (NaN) and document it.

For ml_fmod quotient cap:
c

if (q > 9.22e18) q = 9.22e18;

This is arbitrary. Better to use fmod logic that doesn't cast to integer when quotient is large. Actually, the whole ml_fmod implementation is dubious. For v11S, we should use a proper fmod implementation or use __builtin_fmod? No, the point is to avoid libm dependency. Keep it but fix the cap to be LLONG_MAX related, or use a loop-based reduction for large quotients.
Actually, looking at ml_fmod:
c

double q = x / y;
if (q > 9.22e18) q = 9.22e18;
if (q < -9.22e18) q = -9.22e18;
long long qi = (long long)q;

The cast to long long when q is near 9.22e18 is dangerous because 9.22e18 > 2^63-1 (9.22e18 > 9.22e18? Actually LLONG_MAX is 9.22e18 approx). 2^63-1 = 9.22e18. So the cap is exactly at the limit. This is fragile.
For profiles.h:
c

#define ml_rsqrt(x) ml_fast_rsqrt(x)

This is evil. Replace with:
c

static inline double ml_rsqrt(double x) {
#if defined(MATHLIB_PROFILE_GRAPHICS)
    return ml_fast_rsqrt(x);
#else
    return 1.0 / ml_sqrt(x);
#endif
}

But wait, profiles.h is included everywhere. If ml_rsqrt is a macro, it breaks. So change to inline function.
For test_harness.h:
c

static int g_passed = 0;
static int g_failed = 0;

This prevents aggregation. Change to non-static or use a struct.
For test_quiet.h and test.h:
They are identical! test.h and test_quiet.h have the same SHA256. Merge them.
For legacy/:
legacy/trigonometry.h redefines ml_sin, ml_cos, etc. This is an ODR violation waiting to happen. Delete legacy/ from the build. Move to archive/ or delete entirely.
For compat.h:
It has #warning which is not standard C (GCC/Clang extension). For MSVC, this breaks. Wrap in compiler abstraction or remove.
For math_pi / math_e / M_PI chaos:
Centralize in ml_core.h:
c

#ifndef MATHLIB_PI
#define MATHLIB_PI 3.14159265358979323846
#endif

And use MATHLIB_PI everywhere. Or ML_PI.
Now for the build system:
CMakeLists.txt needs to actually compile the .c files.
Makefile needs updating.
For the exported symbols:
Use a visibility macro. ML_API expands to __attribute__((visibility("default"))) on GCC/Clang. On Windows, __declspec(dllexport) for DLLs or nothing for static libs.
Actually, for a static library, visibility doesn't matter as much. But for shared library future-proofing, use it.
Let me organize the response:
THE V11A3 → V11S REFACTOR BLUEPRINT
I'll structure it as:

    The Core Architectural Lie (Linkage Model)
    The Thread Safety Breach (CPU Dispatch)
    The Platform Portability Wall (Compiler Abstraction)
    Safety as Default (Not a Profile)
    Specific Code Surgery (File by File)
    The New Directory Layout
    Build System Changes
    Execution Order (What to do first)

I need to be extremely concrete. Name the files, show the before/after patterns, specify what moves where.
Let me draft specific file changes:
1. Create include/mathlib/ml_compiler.h
c

#ifndef ML_COMPILER_H
#define ML_COMPILER_H

/* Compiler detection */
#if defined(_MSC_VER)
# define ML_COMPILER_MSVC 1
#elif defined(__GNUC__)
# define ML_COMPILER_GNUC 1
#elif defined(__clang__)
# define ML_COMPILER_CLANG 1
#else
# define ML_COMPILER_UNKNOWN 1
#endif

/* Inline abstraction */
#if ML_COMPILER_MSVC
# define ML_INLINE static __forceinline
# define ML_NOINLINE __declspec(noinline)
# define ML_ALIGN(bytes) __declspec(align(bytes))
#else
# define ML_INLINE static inline __attribute__((always_inline))
# define ML_NOINLINE __attribute__((noinline))
# define ML_ALIGN(bytes) __attribute__((aligned(bytes)))
#endif

/* API export (for future shared library; harmless for static) */
#if ML_COMPILER_MSVC
# define ML_API extern
#else
# define ML_API extern __attribute__((visibility("default")))
#endif

/* Branch hints */
#if defined(__GNUC__) || defined(__clang__)
# define ML_LIKELY(x) __builtin_expect(!!(x), 1)
# define ML_UNLIKELY(x) __builtin_expect(!!(x), 0)
#else
# define ML_LIKELY(x) (x)
# define ML_UNLIKELY(x) (x)
#endif

/* Feature detection (compile-time only for v11S) */
#if defined(__AVX2__) && (defined(__x86_64__) || defined(__i386__))
# define ML_COMPILE_TIME_AVX2 1
#else
# define ML_COMPILE_TIME_AVX2 0
#endif

/* Deprecation */
#if defined(__GNUC__) || defined(__clang__)
# define ML_DEPRECATED(msg) __attribute__((deprecated(msg)))
#else
# define ML_DEPRECATED(msg)
#endif

/* C99 restrict */
#if ML_COMPILER_MSVC
# define ML_RESTRICT __restrict
#else
# define ML_RESTRICT restrict
#endif

#endif

2. Restructure headers: Declaration vs Definition
Pattern:

    ml_trig.h currently has static inline double ml_sin(...) { ... }
    Change to: ML_API double ml_sin(double x);

But wait, ml_trig.h includes internal/minimax.h. If minimax.h also has static inline functions, and ml_trig.h includes it, then including ml_trig.h still pulls in implementations. So internal/minimax.h needs to be split into internal/minimax.h (declarations) and src/minimax.c (definitions). Or just move everything to src/trig.c and keep internal/minimax.h as purely internal declarations not installed.
Actually, the internal/ headers should probably not be in the public include path at all. They should be in src/internal/ or include/internal/ but not installed.
Wait, the current tree has include/mathlib/internal/. If these are installed, users can include them. That's fine, but they should only contain declarations, not definitions, after the refactor.
Let me think about what stays inline in headers:

    ml_fabs, ml_isnan, ml_isinf, ml_copysign, ml_fp_classify, ml_frexp_pure, ml_ldexp_pure - These are 2-3 instructions. Keep as ML_INLINE in ml_core.h.
    ml_round, ml_fmod - These are non-trivial but used everywhere. ml_fmod is actually quite complex. Move to src/core.c? Or keep inline? I'll say move non-trivial ones to src/core.c.
    cplx_add, cplx_sub - trivial. Keep inline.
    cplx_mul - trivial. Keep inline.
    cplx_div - has a branch and division. Move to src/complex.c.
    cplx_abs, cplx_arg - non-trivial (sqrt, atan). Move to src/complex.c.
    cplx_exponential, cplx_logarithm, cplx_power - non-trivial. Move to src/complex.c.
    factorial, npr, ncr - Move to src/combinatorics.c.
    polynomial_eval, polynomial_derivative - small but not hot. Move to src/polynomial.c.
    newton_raphson, bisection, derivative, etc. - Move to src/numerical.c.
    mean, variance, etc. - Move to src/statistics.c.
    ml_sin, ml_cos, ml_tan, ml_atan, ml_asin, etc. - Move to src/trig.c.
    ml_exp, ml_log, ml_pow, etc. - Move to src/exp_log.c.
    ml_solve_v10, ml_lu_decomp_v10 - Move to src/linalg.c.
    fft_execute, ifft_execute - Move to src/fft.c.
    ml_quat_mul, ml_quat_normalize, ml_quat_slerp - Move to src/quaternion.c.
    ml_cordic_sincos, ml_cordic_sincos_fixed - Move to src/cordic.c.
    ml_minimax_sin, ml_minimax_cos - Move to src/minimax.c.
    ml_rem_pio2 - Move to src/payne_hanek.c or src/trig.c.

This is a lot of files. But it's the right way.
3. Fix cpu_dispatch.c
For v11S, eliminate the runtime function pointer. Instead:
c

// cpu_dispatch.c
#include "ml_compiler.h"
#include "cpu_dispatch.h"

ML_API void ml_matmul(const double* ML_RESTRICT A,
                      const double* ML_RESTRICT B,
                      double* ML_RESTRICT C,
                      int N) {
#if ML_COMPILE_TIME_AVX2
    ml_matmul_avx2(A, B, C, N);
#else
    ml_matmul_scalar(A, B, C, N);
#endif
}

Wait, but what if the user compiles on a machine with AVX2 but wants to run on an older machine? For v11S, the answer is: compile twice, or use the scalar build. Runtime dispatch is deferred.
Actually, we can keep the capability query for informational purposes:
c

ML_API int ml_cpu_has_avx2(void) {
#if ML_COMPILE_TIME_AVX2
    return 1;
#else
    return 0;
#endif
}

4. Fix profiles.h
Change macros to inline functions:
c

#ifndef MATHLIB_PROFILES_H
#define MATHLIB_PROFILES_H

#include "ml_compiler.h"

#if defined(MATHLIB_PROFILE_GRAPHICS)
ML_INLINE double ml_rsqrt(double x) {
    return ml_fast_rsqrt(x);
}
#else
ML_INLINE double ml_rsqrt(double x) {
    return 1.0 / ml_sqrt(x);
}
#endif

#endif

5. Fix gamma_new
c

ML_API double ml_gamma(double x) {
    if (x <= 0) return 0.0 / 0.0;
    if (x > 171.0) return 1.0 / 0.0; /* Overflow guard */
    if (x > 2) {
        double result = 1.0;
        while (x > 2) {
            x -= 1;
            result *= x;
        }
        return result * ml_gamma(x);
    }
    /* ... rest ... */
}

Wait, recursion for gamma is bad. Use iterative loop or memoization. Actually for v11S, just add the overflow guard and convert tail recursion to iteration.
6. Fix integral_traditional
c

ML_API double ml_integral_traditional(double a, double b, double exponent, double additive, double d) {
    if (d == 0.0 || ml_isnan(d) || ml_isnan(a) || ml_isnan(b)) return 0.0/0.0;
    if ((d > 0 && a >= b) || (d < 0 && a <= b)) return 0.0;

    double result = 0.0;
    double x = a;
    const int max_steps = 10000000;
    for (int step = 0; step < max_steps; step++) {
        if ((d > 0 && x >= b) || (d < 0 && x <= b)) return result;
        result += (ml_pow(x, exponent) + additive) * d;
        x += d;
    }
    /* Step limit exceeded: return NaN to signal failure */
    return 0.0 / 0.0;
}

7. Fix ml_fmod
The current implementation is actually okay-ish but the cap is magic. Better:
c

ML_API double ml_fmod(double x, double y) {
    if (ml_isnan(x) || ml_isnan(y) || ml_isinf(x)) return 0.0/0.0;
    if (ml_isinf(y)) return x;
    if (y == 0.0) return 0.0/0.0;

    double abs_y = ml_fabs(y);
    double r = ml_fabs(x);
    if (r < abs_y) return x; /* Early exit */

    /* For very large quotients, avoid integer overflow */
    if (r > abs_y * 9.22e18) {
        /* Use iterative subtraction for pathological cases */
        while (r >= abs_y) {
            r -= abs_y;
        }
    } else {
        long long q = (long long)(r / abs_y);
        r -= (double)q * abs_y;
    }

    return ml_copysign(r, x);
}

Actually this is better but still has issues. For v11S, document the limitation.
8. Test harness
Change test_harness.h:
c

typedef struct {
    int passed;
    int failed;
} ml_test_ctx_t;

#define ASSERT_TRUE(ctx, cond, msg) do { \
    if (cond) { (ctx)->passed++; } \
    else { (ctx)->failed++; printf("  [FAIL] %s (Line %d)\n", msg, __LINE__); } \
} while(0)

And pass &ctx around.
9. Merge duplicate headers
test.h and test_quiet.h are identical. Delete one, rename the other.
10. Versioning
Standardize to MATHLIB_VERSION_MAJOR 11, MATHLIB_VERSION_MINOR 0, MATHLIB_VERSION_PATCH 0 for v11S. Add include/mathlib/version.h.
11. Legacy
Delete legacy/ entirely or move to archive/. Do not ship with v11S.
12. compat.h
Either remove or make it opt-in. The #warning is not portable. Wrap:
c

#if defined(__GNUC__) || defined(__clang__)
#warning "Deprecated..."
#endif

Now let me structure the response. It should be:

    Introduction acknowledging the roleplay context but immediately diving into technical specifics
    The Refactor Philosophy (what v11S means)
    Phase 1: The Linkage Model (the biggest change)
    Phase 2: Compiler Abstraction (unblocking Yellow team)
    Phase 3: Thread Safety & CPU Dispatch
    Phase 4: Safety by Default
    Phase 5: File-by-File Surgery (the meat)
    Phase 6: Build System Overhaul
    Phase 7: Testing Infrastructure
    Phase 8: The Math Diary (non-code but critical)
    Summary/Execution Order

I need to be very direct. "Do this. Not that. Here is the exact file layout."
Let me write this out.
I look at you. The booth is quieter now—day two of CES, the hype is thinning, the hangovers are setting in. I’m still in the same blazer, but I’ve got a coffee now, and I’m not leaving until you understand exactly what has to happen. No performance. No investor pitch. Just engineering.
The V11S Refactor: Architectural Blueprint
V11S means "Stable." Stable does not mean "it compiles." It means: a human can reason about the binary, a linker can patch it, and a thread can call it without praying. Here is the exact surgery.
Phase 1: Kill the Header-Only Lie
The Problem: Every non-trivial function is static inline in a header. src/mathlib.c is just a list of #includes. libmathc.a exports three symbols. It is not a library. It is a distributed code generator.
The Rule for V11S: If a function does more than bit-masking, branching, or a single arithmetic operation, it lives in a .c file and is exported via ML_API.
What stays inline in headers (trivial only):

    ml_fabs, ml_isnan, ml_isinf, ml_fp_classify, ml_copysign
    ml_ldexp_pure, ml_frexp_pure (they are pure bit-shifts)
    cplx_add, cplx_sub, cplx_mul (simple arithmetic)
    ML_TENSOR_AT, ml_tensor_view, ml_workspace_alloc (struct accessors)

What moves to .c files immediately:

    All transcendental: ml_sin, ml_cos, ml_exp, ml_log, ml_atan, etc.
    All linear algebra: ml_lu_decomp_v10, ml_solve_v10
    All FFT: fft_execute, ifft_execute
    All complex non-trivial: cplx_div, cplx_abs, cplx_arg, cplx_exponential, cplx_logarithm, cplx_power
    All numerical methods: newton_raphson, bisection, integral_simpson, etc.
    All statistics: mean, variance, stddev, linear_regression
    All optimization: optimize_golden, optimize_gradient_descent
    All quaternion: ml_quat_mul, ml_quat_normalize, ml_quat_slerp
    All minimax/CORDIC internals: ml_minimax_sin, ml_cordic_sincos, ml_rem_pio2

New file layout under src/:
plain

src/
  core.c          (ml_fmod, ml_round, ml_sqrt - the non-trivial core)
  trig.c          (ml_sin, ml_cos, ml_tan, ml_atan, ml_asin, etc.)
  exp_log.c       (ml_exp, ml_log, ml_pow, hyperbolics)
  minimax.c       (ml_minimax_sin_raw, ml_minimax_cos_raw)
  cordic.c        (ml_cordic_sincos, ml_cordic_sincos_fixed)
  payne_hanek.c   (ml_rem_pio2)
  linalg.c        (ml_lu_decomp_v10, ml_solve_v10)
  fft.c           (fft_execute, ifft_execute)
  complex.c       (cplx_div, cplx_abs, cplx_arg, cplx_exp, cplx_log, cplx_pow)
  combinatorics.c (factorial, npr, ncr)
  quadratics.c    (ml_equation, ml_formula_pos, ml_formula_neg)
  polynomial.c    (polynomial_eval, polynomial_derivative, polynomial_newton)
  numerical.c     (newton_raphson, bisection, derivative, integral_simpson)
  statistics.c    (mean, variance, stddev, binomial_pmf, normal_pdf, linear_regression)
  integral.c      (factorial_float, integral_traditional, gamma_new)
  ode.c           (ode_euler, ode_rk4)
  optimization.c  (optimize_golden, optimize_gradient_descent)
  quaternion.c    (ml_quat_mul, ml_quat_normalize, ml_quat_slerp)
  simd_dispatch.c (ml_simd_batch_poly, ml_simd_batch_rsqrt - scalar path)
  cpu_caps.c      (ml_cpu_has_avx2, etc. - informational only)
  compat.c        (legacy shim definitions)
  mathlib.c       (DELETED - no longer needed)

Header changes:

    ml_trig.h now contains only declarations:
    c

    ML_API double ml_sin(double x);
    ML_API double ml_cos(double x);
    ML_API double ml_tan(double x);
    ML_API double ml_atan(double x);
    ML_API double ml_asin(double x);
    ML_API double ml_acos(double x);
    ML_API double ml_atan2(double y, double x);
    ML_API double ml_acot(double x);

    internal/minimax.h keeps the coefficient tables (minimax_sin_coeffs[]) but removes the static inline function bodies. The bodies move to src/minimax.c.
    Same pattern for every other header.

The ML_API macro (in include/mathlib/ml_compiler.h):
c

#if defined(_WIN32) && defined(MATHLIB_BUILD_DLL)
#  define ML_API __declspec(dllexport)
#elif defined(_WIN32) && defined(MATHLIB_USE_DLL)
#  define ML_API __declspec(dllimport)
#elif defined(__GNUC__) && (__GNUC__ >= 4)
#  define ML_API extern __attribute__((visibility("default")))
#else
#  define ML_API extern
#endif

Phase 2: Create ml_compiler.h (Yellow Team Pre-Flight)
This is non-negotiable. No file in your codebase may use __attribute__, __builtin_*, __asm__, or _mm256_* directly except through this header.
include/mathlib/ml_compiler.h:
c

#ifndef ML_COMPILER_H
#define ML_COMPILER_H

/* Compiler identity */
#if defined(_MSC_VER)
#  define ML_COMPILER_MSVC 1
#elif defined(__clang__)
#  define ML_COMPILER_CLANG 1
#elif defined(__GNUC__)
#  define ML_COMPILER_GNUC 1
#endif

/* Inline abstraction */
#if ML_COMPILER_MSVC
#  define ML_INLINE static __forceinline
#  define ML_NOINLINE __declspec(noinline)
#  define ML_ALIGN(n) __declspec(align(n))
#else
#  define ML_INLINE static inline __attribute__((always_inline))
#  define ML_NOINLINE __attribute__((noinline))
#  define ML_ALIGN(n) __attribute__((aligned(n)))
#endif

/* API linkage */
#if defined(_WIN32) && defined(MATHLIB_BUILD_DLL)
#  define ML_API __declspec(dllexport)
#elif defined(_WIN32) && defined(MATHLIB_USE_DLL)
#  define ML_API __declspec(dllimport)
#elif defined(__GNUC__) && (__GNUC__ >= 4)
#  define ML_API extern __attribute__((visibility("default")))
#else
#  define ML_API extern
#endif

/* Branch prediction hints */
#if defined(__GNUC__) || defined(__clang__)
#  define ML_LIKELY(x) __builtin_expect(!!(x), 1)
#  define ML_UNLIKELY(x) __builtin_expect(!!(x), 0)
#else
#  define ML_LIKELY(x) (x)
#  define ML_UNLIKELY(x) (x)
#endif

/* Restrict pointer */
#if defined(__STDC_VERSION__) && (__STDC_VERSION__ >= 199901L)
#  define ML_RESTRICT restrict
#elif defined(_MSC_VER)
#  define ML_RESTRICT __restrict
#else
#  define ML_RESTRICT
#endif

/* Target attributes (GCC/Clang only; MSVC ignores) */
#if defined(__GNUC__) || defined(__clang__)
#  define ML_TARGET_AVX2 __attribute__((target("avx2,fma")))
#else
#  define ML_TARGET_AVX2
#endif

/* Compile-time feature detection */
#if defined(__AVX2__) && (defined(__x86_64__) || defined(__i386__))
#  define ML_COMPILE_TIME_AVX2 1
#else
#  define ML_COMPILE_TIME_AVX2 0
#endif

/* Deprecation */
#if defined(__GNUC__) || defined(__clang__)
#  define ML_DEPRECATED(msg) __attribute__((deprecated(msg)))
#else
#  define ML_DEPRECATED(msg)
#endif

/* Constructor (for library init; MSVC must use #pragma init_seg or DllMain) */
#if defined(__GNUC__) || defined(__clang__)
#  define ML_CTOR __attribute__((constructor))
#else
#  define ML_CTOR
#endif

#endif

Refactoring mandate: Search the entire codebase for __attribute__, __builtin_, __asm__, immintrin.h (outside of simd_bare_metal.h and simd_batch.h). Every hit must be routed through ml_compiler.h. This is a perfect Qwen task: mechanical, high-volume, zero math required.
Phase 3: Execute the Global State
Current crime:
c

ml_cpu_caps_t g_ml_cpu_caps = {0};
ml_matmul_func_t g_ml_matmul = ml_matmul_scalar;

Lazy init via if (!initialized). This is a data race. Your Design Contract says "No Global State."
V11S solution: Compile-time dispatch only.
Delete the runtime function pointer. Delete g_ml_matmul. Delete ml_init_cpu_dispatch().
src/matmul.c:
c

#include "ml_compiler.h"
#include "cpu_dispatch.h"

ML_API void ml_matmul(const double* ML_RESTRICT A,
                      const double* ML_RESTRICT B,
                      double* ML_RESTRICT C,
                      int N) {
#if ML_COMPILE_TIME_AVX2
    ml_matmul_avx2(A, B, C, N);
#else
    ml_matmul_scalar(A, B, C, N);
#endif
}

src/cpu_caps.c (informational only, no dispatch):
c

#include "ml_compiler.h"
#include "cpu_dispatch.h"

ML_API int ml_cpu_has_avx2(void) { return ML_COMPILE_TIME_AVX2; }
ML_API int ml_cpu_has_fma(void) {
#if defined(__FMA__) && (defined(__x86_64__) || defined(__i386__))
    return 1;
#else
    return 0;
#endif
}

Why this is correct for V11S: Runtime CPU dispatch is a v12 feature. It requires either ifunc (GNU only), stdatomic (C11), or pthread_once. You are writing C99. Defer it. If a user wants AVX2, they compile with -mavx2 -mfma. If they want portable binaries, they compile the scalar path. This is how OpenBLAS, FFTW, and every serious numerical library handles it.
Phase 4: Safety Is Not a Profile
Current: NULL checks, dimension validation, and canary verification are gated behind #if defined(MATHLIB_PROFILE_HARDENED).
This is insane. A "hardened perimeter" should not be optional. It should be the default, with an opt-out for embedded systems that have already validated their inputs.
V11S rule: All ML_ERR_INVALID_ARG, NULL pointer checks, and dimension checks are always compiled. The MATHLIB_PROFILE_HARDENED flag adds only:

    Memory canaries in the workspace allocator
    Address sanitizer / UBSan flags
    Extra paranoid checks (e.g., NaN poisoning on workspace reset)

Refactor ml_linalg.h and ml_tensor.h:
Remove #if defined(MATHLIB_PROFILE_HARDENED) around the NULL checks. Keep it only around the canary magic number.
Before:
c

#if defined(MATHLIB_PROFILE_HARDENED)
    if (n <= 0 || A.data == NULL || LU.data == NULL || P == NULL) return ML_ERR_INVALID_ARG;
#endif

After:
c

    if (ML_UNLIKELY(n <= 0 || A.data == NULL || LU.data == NULL || P == NULL))
        return ML_ERR_INVALID_ARG;

Note ML_UNLIKELY—this tells the compiler the error path is cold, improving hot-path instruction cache density.
Phase 5: Specific Code Surgery (The Bugs)
1. gamma_new Stack Overflow
Current:
c

if (x > 2) return (x - 1) * gamma_new(x - 1);

For x = 10000, you recurse 9998 times. Stack overflow.
Fix in src/integral.c:
c

ML_API double ml_gamma(double x) {
    if (x <= 0.0) return 0.0 / 0.0;
    if (x > 171.0) return 1.0 / 0.0; /* 172! overflows double */

    if (x > 2.0) {
        double result = 1.0;
        while (x > 2.0) {
            x -= 1.0;
            result *= x;
        }
        return result * ml_gamma(x);
    }
    if (x < 1.0) return ml_gamma(x + 1.0) / x;
    /* ... polynomial for [1,2] ... */
}

Even better: eliminate the remaining recursion for x < 1.0 too. Use a loop.
2. integral_traditional Silent Failure
Current: returns partial result after 10,000,000 steps. Wrong.
Fix in src/integral.c:
c

ML_API double ml_integral_traditional(double a, double b, double exponent, double additive, double d) {
    if (d == 0.0 || ml_isnan(d) || ml_isnan(a) || ml_isnan(b)) return 0.0 / 0.0;
    if ((d > 0 && a >= b) || (d < 0 && a <= b)) return 0.0;

    double result = 0.0;
    double x = a;
    const int max_steps = 10000000;

    for (int step = 0; step < max_steps; step++) {
        if ((d > 0 && x >= b) || (d < 0 && x <= b))
            return result;
        result += (ml_pow(x, exponent) + additive) * d;
        x += d;
    }
    /* Step limit exceeded: signal failure */
    return 0.0 / 0.0;
}

3. ml_fmod Integer Overflow
Current caps at 9.22e18 and casts to long long. This is exactly at LLONG_MAX and is fragile.
Fix in src/core.c:
c

ML_API double ml_fmod(double x, double y) {
    if (ml_isnan(x) || ml_isnan(y) || ml_isinf(x)) return 0.0 / 0.0;
    if (ml_isinf(y)) return x;
    if (y == 0.0) return 0.0 / 0.0;

    double abs_y = ml_fabs(y);
    double r = ml_fabs(x);

    if (r < abs_y) return ml_copysign(r, x);

    /* Pathological case: quotient too large for integer cast */
    if (r > abs_y * 9.22e18) {
        while (r >= abs_y) {
            r -= abs_y;
        }
        return ml_copysign(r, x);
    }

    long long q = (long long)(r / abs_y);
    r -= (double)q * abs_y;

    /* Correct possible rounding error */
    if (r >= abs_y) r -= abs_y;

    return ml_copysign(r, x);
}

4. profiles.h Macro Poisoning
Current:
c

#define ml_rsqrt(x) ml_fast_rsqrt(x)

This breaks ml_rsqrt(a++) (double increment) and prevents taking the address of ml_rsqrt.
Fix:
c

#ifndef MATHLIB_PROFILES_H
#define MATHLIB_PROFILES_H

#include "ml_compiler.h"

#if defined(MATHLIB_PROFILE_GRAPHICS)
ML_INLINE double ml_rsqrt(double x) { return ml_fast_rsqrt(x); }
#else
ML_INLINE double ml_rsqrt(double x) { return 1.0 / ml_sqrt(x); }
#endif

#endif

5. compat.h Non-Portable Warning
#warning is not ISO C. MSVC chokes.
Fix:
c

#if defined(__GNUC__) || defined(__clang__)
#warning "MathLib V1.1: Using deprecated V1.0 API..."
#endif

6. legacy/ Directory
Delete it. Move to archive/legacy-v10/ outside the build tree. legacy/trigonometry.h redefines ml_sin and will cause ODR violations if accidentally included.
7. test.h / test_quiet.h Duplication
They have identical SHA256 hashes. Delete one. Rename the survivor to test_assertions.h.
8. test_harness.h Static Globals
c

static int g_passed = 0; /* Cannot aggregate across TUs */

Fix:
c

typedef struct {
    int passed;
    int failed;
} ml_test_ctx_t;

#define ASSERT_TRUE(ctx, cond, msg) do { \
    if (cond) { (ctx)->passed++; } \
    else { (ctx)->failed++; printf("  [FAIL] %s (Line %d)\n", msg, __LINE__); } \
} while(0)

Phase 6: Build System Overhaul
CMakeLists.txt changes:

    Add ml_compiler.h to the public headers.
    Replace the fake mathlib.c with the real source files.
    Remove MATHLIB_ENABLE_LEGACY option. Legacy is gone.
    Add MATHLIB_BUILD_DLL option for Windows (Yellow team future).
    Enforce C_STANDARD 99 but allow C_STANDARD_REQUIRED OFF for MSVC if needed (MSVC is technically C89 with some C99 features).

Makefile changes:

    Add all new .c files to SRC.
    Add -fvisibility=hidden by default so only ML_API symbols are exported (GNU/Clang).

New include/mathlib/version.h:
c

#ifndef MATHLIB_VERSION_H
#define MATHLIB_VERSION_H

#define MATHLIB_VERSION_MAJOR 11
#define MATHLIB_VERSION_MINOR 0
#define MATHLIB_VERSION_PATCH 0
#define MATHLIB_VERSION_STRING "11.0.0"

#endif

Phase 7: The Math Diary (Non-Code, Required)
Before any module is marked STABLE in API_STATUS.md, create docs/contracts/<module>.md:
Example: docs/contracts/trig_sin.md
Markdown
Copy
Code Preview

# Contract: ml_sin

## Identity
Implements sin(x) for IEEE-754 double precision.

## Algorithm
1. Payne-Hanek range reduction: x → y in [-π/4, π/4], quadrant n.
2. If n=0: ml_minimax_sin_raw(y)
3. If n=1: ml_minimax_cos_raw(y)
4. If n=2: -ml_minimax_sin_raw(y)
5. If n=3: -ml_minimax_cos_raw(y)

## Error Budget
- Reduction error: < 0.5 ULP (exact π/2 split)
- Polynomial error: < 0.5 ULP (9th-degree minimax on [-π/4, π/4])
- Total guaranteed: < 1 ULP vs infinite-precision reference for |x| < 1e18

## Failure Modes
- |x| > 1e18: Reduction loses precision. Returns inexact result (not NaN).
- NaN/Inf input: Returns NaN.
- Subnormal output: Possible. Handled by IEEE-754 naturally.

## Oracle Test
See `scripts/oracles/sin_oracle.py` (mpmath, 50 digits).

You write this. Not Qwen. Qwen can draft it, but you must verify every sentence. This is how you reverse-engineer without lying to yourself.
Phase 8: Execution Order (What You Do Tomorrow)
Week 1 (Qwen - Blue Team):

    Generate ml_compiler.h.
    Refactor every __attribute__, __builtin_, __asm__ to use ML_* macros.
    Create the new src/*.c files from the existing static inline implementations.
    Strip implementations from headers, leaving only ML_API declarations.

Week 2 (You + Red Team):

    Write the Math Diary for ml_trig, ml_exp_log, ml_linalg, fft.
    Fix gamma_new, integral_traditional, ml_fmod.
    Merge test.h/test_quiet.h. Fix test harness globals.
    Delete legacy/. Fix compat.h.

Week 3 (Build + CI):

    Update CMakeLists.txt and Makefile.
    Verify nm libmathc.a | grep " T " shows 50+ exported symbols.
    Run the full fuzz suite. If anything fails, the module stays in A1.

Week 4 (Yellow Team - Benched until this is done):

    Only after ml_compiler.h is proven on Linux with GCC/Clang do you hand it to Copilot for the MSVC paths.
    Yellow team implements ML_COMPILER_MSVC branches.

The Final Verdict
V11S is not a feature release. It is a contract release. The code already exists. The math already exists. What does not exist is the boundary between "this is a header" and "this is a library," between "this works on my machine" and "this links on any machine," between "this passes fuzzing" and "this has a human-understood error budget."
Do this refactor. Do not add a single new function until it is done. Not CORDIC improvements. Not new SIMD paths. Not Windows support. Nothing.
Because if you add one more static inline transcendental function to this codebase before fixing the linkage model, you are not building a library. You are building a very fast, very well-tested, very unmaintainable pile of compiler output.
I finish my coffee. It’s cold now.
Get the symbols exported. Get the contracts written. Then come back and tell me what the 53rd bit of ml_rem_pio2 does.
I walk away. The afternoon sessions are starting.
 the next day, again:

now I ask you for this: go into the ultra nitty gritties, you no longer need to care about scope and the wider picture at all
Edit
Copy
Share
Confirm Correctness of FFT Bit‑Reversal Loop
The user wants me to go into the "ultra nitty gritties" of the codebase, no longer caring about scope and wider picture. They want the deepest level of code review, line-by-line, function-by-function, finding every bug, every undefined behavior, every inefficiency, every standard violation, every logic error, every typo, every inconsistency in the actual code that was provided in the project dump.
I need to go through the uploaded files with a fine-tooth comb and extract every specific issue I can find. This is a massive code dump, so I need to be systematic.
Let me go through the files one by one and note specific issues:
v11A3/compat.h

    Defines macros like #define sine(x) ml_sin(x) - these are unsafe macros without parentheses around arguments in the replacement? Actually they do have parentheses. But #define arctangent2(y, x) ml_atan2(y, x) - if someone writes arctangent2(a,b,c) it won't expand correctly but that's not a real issue. The bigger issue is that compat.h uses #warning which is not standard C (it's a GCC/Clang extension). MSVC doesn't support it.

v11A3/soak_test.sh

    ./build/fuzz_god_mode - assumes build directory exists. No mkdir -p build.
    python3 tests/run_fuzz_vault.py - only runs on failure, but the vault script itself generates a stub that doesn't actually reproduce anything meaningful.

v11A3/Makefile

    CFLAGS = -std=c99 -O3 -Wall -Wextra -fno-fast-math -ffp-contract=off -Iinclude/mathlib - missing -Iinclude for headers that include other headers like #include "ml_core.h" from inside include/mathlib/. Actually the headers use #include "ml_core.h" style, and since they're in include/mathlib/, if a file includes ml_trig.h, the compiler needs to find ml_core.h in the same directory. With -Iinclude/mathlib, when processing ml_trig.h, it can find ml_core.h because it's in the same directory as the including file? Actually no - -Iinclude/mathlib means the preprocessor searches include/mathlib for quoted includes. If ml_trig.h has #include "ml_core.h", and ml_trig.h is in include/mathlib/, the preprocessor first searches the directory containing ml_trig.h, so it finds ml_core.h. So that works.
    Missing -Wmissing-prototypes and -Wstrict-prototypes which would catch the missing declarations for non-static functions.
    test_modular target depends on $(TESTS) but $(TESTS) are not in the all target. Also test_modular runs a python script but the binaries might not be in PATH.
    clean doesn't clean clean_modular targets.

v11A3/CMakeLists.txt

    project(mathlib_v1_0 C) - version inconsistency.
    MATHLIB_ENABLE_LEGACY option has an endif() with wrong indentation (not a bug, just sloppy).
    add_library(mathc STATIC src/mathlib.c src/cpu_dispatch.c) - with the refactor, this needs to change, but as-is, src/mathlib.c just includes headers with static inline functions, so the library is nearly empty.
    install(EXPORT mathlibTargets FILE mathlibTargets.cmake NAMESPACE mathlib:: DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/mathlib) - missing include(CMakePackageConfigHelpers) and config file generation. The export alone is not enough for find_package(mathlib REQUIRED) to work. You need a mathlibConfig.cmake file.
    MATHLIB_SANITIZERS adds -fsanitize=address,undefined but doesn't check if the compiler supports it.
    Missing target_compile_definitions for profiles on the library itself? The profile defines are added via add_compile_definitions which affects all targets, but the library mathc doesn't get the profile flags explicitly.

v11A3/tests/test_trig.c

    #include "ml_exp_log.h" but only uses ml_log and ml_exp - fine.
    ASSERT_NEAR(ml_log(1.0), 0.0, 1e-15, "log(1)"); - ml_log returns -inf for 0.0, NaN for negative, but 1.0 is fine.
    ASSERT_NEAR(ml_exp(0.0), 1.0, 1e-15, "exp(0)"); - fine.

v11A3/tests/test_core.c

    ASSERT_TRUE(ml_isnan(0.0/0.0), "NaN detection"); - 0.0/0.0 is undefined behavior in C if FPE is enabled, but in IEEE-754 it's NaN. However, strictly speaking, 0.0/0.0 is not guaranteed to be NaN in C99 unless #pragma STDC FENV_ACCESS or similar? Actually IEEE-754 annex says it should be NaN, but C99 without IEEE-754 support could do anything. In practice it's fine.
    ASSERT_TRUE(ml_isinf(1.0/0.0), "Inf detection"); - same issue.

v11A3/tests/fuzz_boundary_gauntlet.c

    ml_q16_16_t max_val = 2147483647; - this is INT32_MAX. ml_q16_16_t is int32_t. 2147483647 is fine but (-2147483647 - 1) is a weird way to write INT32_MIN. The literal -2147483648 doesn't fit in int32_t as a positive literal, so (-2147483647 - 1) is the correct way to write it without overflow. Good.
    CHECK(ml_fixed_mul(max_val, max_val) == 2147483647, ...) - ml_fixed_mul does ((int64_t)a * b) >> 16. INT32_MAX * INT32_MAX = ~4.6e18. >> 16 = ~7e13. Then it checks > INT32_MAX and saturates. But wait: INT32_MAX * INT32_MAX as int64_t is 4611686014132420609. >> 16 is 70368744177663. That is way bigger than INT32_MAX, so it saturates. Good.
    CHECK(ml_fixed_mul(min_val, max_val) == (-2147483647 - 1), ...) - INT32_MIN * INT32_MAX = -4611686016279904256. >> 16 = -70368744174176. That is less than INT32_MIN, so saturates to INT32_MIN. Good.
    CHECK(ml_fixed_mul(min_val, min_val) == 2147483647, ...) - INT32_MIN * INT32_MIN = 4611686018427387904. >> 16 = 70368744177664. Saturates to INT32_MAX. Good.
    CHECK(ml_fixed_mul(two, three) == (6 << 16), "2.0 * 3.0 == 6.0"); - two = 2 << 16 = 131072. three = 3 << 16 = 196608. Product = 25769803776. >> 16 = 393216 = 6 << 16. Good.
    test_tensor_hilbert: char scratchpad[1024 * 1024]; - 1MB stack allocation. That's fine for most systems but could blow stack on embedded. The function doesn't check if ml_workspace_alloc returns NULL properly? Actually ml_solve_v10 returns status, and it checks status == ML_SUCCESS. But ml_workspace_alloc in hardened mode checks canary, but in default mode it just checks bounds. If the scratchpad is too small, ml_workspace_alloc returns NULL, and ml_solve_v10 should return ML_ERR_WORKSPACE. But the test doesn't check if the scratchpad size is sufficient for n=4. 4x4 double LU needs 16 doubles + 4 ints + 4 doubles = ~168 bytes + alignment. 1MB is plenty.
    test_fft_parseval: srand(time(NULL)); - time(NULL) returns time_t, srand takes unsigned int. Potential truncation on 64-bit systems where time_t is 64-bit. Not a bug but a warning.
    cplx time_domain[64]; - VLA? No, 64 is constant. Fine.
    freq_energy /= n; - Parseval's theorem for DFT: energy in time domain = (1/N) * energy in freq domain. The code computes time_energy = sum |x|^2, freq_energy = sum |X|^2 / N. That matches. Good.

v11A3/tests/test_quiet.h / test.h

    extern int tests_passed; - defined in test.c. But if multiple files include this, they all reference the same globals. However, test_quiet.h and test.h are identical. This is a maintenance hazard.
    CHECK_NEAR(got,expected) uses TEST_EPSILON = 1e-9 without relative scaling. For values near 1e-12, this is absolute and will pass garbage. But it's a test macro, not production code.

v11A3/tests/test_harness.h

    static int g_passed = 0; - as noted before, static in header means each TU gets its own copy. If you include this in multiple test files and try to link them together, you get multiple copies. But each test file has its own main(), so it's fine for separate executables. Not a bug per se, but a design limitation.

v11A3/tests/test_linalg.c

    char scratchpad[4096]; - 4KB stack. Fine.
    double A[4] = {2, 0, 0, 2}; - diagonal matrix. Easy case. Doesn't test non-trivial pivoting.

v11A3/tests/test.c

    Massive monolithic test. Includes profiles.h and simd_batch.h but simd_batch.h uses AVX2 intrinsics if available. The test includes it unconditionally.
    volatile double sink = 0.0; - used to prevent optimization. Good.
    CHECK_NEAR_LOOSE(ml_exp(1.25531825704261), 3.50895494627857, 1e-7); - repeated three times. Why? Probably copy-paste artifact from AI generation.
    CHECK_NEAR_LOOSE(ml_log(36.1519553264491), 3.58773103639371, 1e-7); - repeated three times.
    CHECK_NEAR_LOOSE(ml_pow(5.53864632208984, -1.17703005732533), 0.133349557404308, 1e-7); - repeated three times.
    CHECK_NEAR(ml_logb(8, 2), 3); - ml_logb uses ml_log(x) / ml_log(b). ml_log(8) / ml_log(2). Should be exactly 3 but floating point might give 2.999999... The CHECK_NEAR macro uses absolute epsilon 1e-9, so it might pass.
    CHECK_NEAR(cplx_arg((cplx) {0, 1}), math_pi / 2); - cplx_arg for real==0 && imag>0 returns math_pi / 2. Good.
    CHECK_NEAR(cplx_arg((cplx) {0, 0}), 0.0 / 0.0); - Wait, cplx_arg for real==0 && imag==0 returns 0.0 / 0.0 (NaN). But CHECK_NEAR does ml_fabs(got - expected) < 1e-9. NaN - NaN is NaN. ml_fabs(NaN) is NaN. NaN < 1e-9 is false. So this test FAILS but it's not in the file... wait, let me check. Looking at test.c... I don't see a test for cplx_arg({0,0}). Good, because it would fail the macro.
    r = cplx_exponential((cplx) {0, math_pi}); - cplx_exponential computes ml_exp(0) * cos(pi) and ml_exp(0) * sin(pi). ml_sin(pi) might not be exactly 0 due to approximation. CHECK_NEAR(r.imag, 0); - this could fail if ml_sin(pi) returns 1e-16 instead of 0, because CHECK_NEAR uses 1e-9 absolute. Actually 1e-9 is generous. But wait, ml_sin(pi) is tested elsewhere with epsilon 1e-14. If ml_sin(pi) returns ~1e-16, CHECK_NEAR with 1e-9 passes. Fine.
    CHECK_NEAR_LOOSE(ml_fma(2.0, 3.0, 1e-16), 6.0, 1e-13); - ml_fma is software FMA. 2.0*3.0 = 6.0. 6.0 + 1e-16 = 6.0 in double precision (1e-16 is below the ulp of 6.0). So ml_fma returns 6.0. Expected is 6.0. The tolerance is 1e-13. This passes, but it's a weak test. It doesn't actually test that the FMA captured the low bit.
    ml_cordic_sincos(math_pi / 2.0, &c_sin, &c_cos); - CHECK_NEAR_LOOSE(c_cos, 0.0, 1e-6); - relaxed because CORDIC converges slowly near pi/2. But ml_cordic_sincos in cordic.h does range reduction to [-pi, pi], then quadrant mapping. For theta=pi/2, it maps to theta = M_PI - theta? No: if (theta > M_PI / 2.0) { theta = M_PI - theta; negate_cos = 1; }. For theta = pi/2 exactly, theta > M_PI/2.0 is false (since it's equal). So it enters CORDIC with z=pi/2. The CORDIC loop has 24 iterations. At i=23, the atan is ~1.19e-7. So after 24 iterations, z might not converge to exactly 0, leaving residual error. cos(pi/2) might be ~1e-7. The test allows 1e-6. Fine.
    double massive_angle = 100000.0 * math_pi + 0.5; - ml_rem_pio2(massive_angle, &reduced_y). 100000.0 * math_pi is about 314159.265... The ml_rem_pio2 uses ml_round(x * 0.63661977236758134308). For large x, x * 2/pi is large. ml_round casts to long long internally if x < 9.22e18. Here it's ~200000, so fine. The reduced value should be ~0.5. But 100000 mod 4 is 0, so quadrant is 0. The test checks reduced_y against 0.5 with 1e-10 tolerance. However, ml_rem_pio2 computes r1 = x - fn * PIO2_HI and r2 = -fn * PIO2_LO. For large fn, fn * PIO2_LO might have catastrophic cancellation? Actually PIO2_LO is very small (~6e-17). fn * PIO2_LO for fn~200000 is ~1.2e-11. The subtraction r1 + r2 should preserve this. But r1 itself is computed as x - fn * PIO2_HI. PIO2_HI is ~1.5708. fn * PIO2_HI for fn=200000 is ~314159.265... x is 100000*pi + 0.5 ≈ 314159.2653589793 + 0.5. The product fn * PIO2_HI might not exactly equal 100000 * math_pi because PIO2_HI is an approximation. The difference is exactly what PIO2_LO is meant to correct. So the result should be accurate. But the test expects reduced_y == 0.5 with 1e-10. If the reduction is exact, yes. But 100000.0 * math_pi is computed in double precision first. math_pi is a double literal. 100000.0 * math_pi has rounding error. Then ml_rem_pio2 reduces that already-rounded value. The "true" mathematical value of 100000*pi + 0.5 reduced mod pi/2 is exactly 0.5, but the double-precision input is not exactly that. So the reduced value might not be exactly 0.5. The test allows 1e-10, which is reasonable.
    ml_minimax_sin(math_pi / 6.0) - CHECK_NEAR_LOOSE(..., 0.5, 1e-7). math_pi / 6.0 is ~0.5236. ml_minimax_sin reduces via ml_rem_pio2. pi/6 is less than pi/4, so it goes to ml_minimax_sin_raw. The polynomial is degree 9 in x^2. Should be quite accurate. 1e-7 is loose.

v11A3/tests/run_tests.py

    subprocess.run([f"./{t}"], ...) - assumes the test binaries are in the current directory. If run from a different directory, it fails.
    No timeout on subprocess. If a test hangs, CI hangs.

v11A3/tests/fuzz_god_mode.c

    rand_double() returns rand() / RAND_MAX * 2.2250738585072014e-308 for subnormal boundary. RAND_MAX is typically 2^31-1. So the smallest value is ~1e-308 / 2^31 ≈ 1e-317, which is below the smallest subnormal (~5e-324). Actually rand() / RAND_MAX is at least 1/RAND_MAX ≈ 4.6e-10. Times 2.2e-308 = ~1e-317. That's still above 5e-324? No, 1e-317 is much larger than 5e-324. So it generates subnormals but not the absolute smallest.
    rand_double() type 3: rand() / RAND_MAX * 2.2250738585072014e-308 - this is meant to be subnormal boundary, but rand()/RAND_MAX is uniform in [0,1], so this generates uniform subnormals in [0, 2.2e-308]. Fine.
    test_ieee754_specials(): ml_sin(x); ml_cos(x); ml_tan(x); for x = 0.0. But ml_tan(0.0) calls ml_sin and ml_cos. ml_sin(0.0) returns ml_minimax_sin(0.0) which calls ml_rem_pio2(0.0, &y). ml_rem_pio2 does ml_round(0.0 * 2/pi) = 0. r1 = 0.0 - 0.0 * PIO2_HI = 0.0. r2 = -0.0 * PIO2_LO = -0.0. *y = 0.0 + (-0.0) = 0.0. n = 0 & 3 = 0. ml_minimax_sin_raw(0.0) returns 0.0. Good.
    test_trig_identities(): double pyth = s*s + c*c; - for large x, s and c might have large errors. The tolerance is 1e-10, which is very loose for double precision.
    test_exp_log_properties(): double prod = x * y; - if x and y are huge, prod might overflow to inf. Then ml_log(prod) is inf. ml_log(x) + ml_log(y) might be finite + finite = inf? Actually if x and y are both ~1e200, prod overflows. ml_log(x) is ~460. Sum is ~920. ml_log(prod) is ml_log(inf) which is inf. The check ml_fabs(log_prod - log_sum) < ml_fabs(ml_log(x)) * 1e-9 + 1e-9 becomes inf - 920 < ... which is inf < finite, which is false. So this test can fail when prod overflows. However, the code has a guard: if (x > 2.225...e-308 && y > ... && prod > ... && !ml_isinf(prod)). So it skips if prod is inf. Good.
    test_catastrophic_cancellation(): formula_pos(1.0, 1e8, 1.0) - formula_pos uses ml_formula_pos. disc = 1e16 - 4.0. ml_sqrt(disc) ≈ 99999999.99999998. b >= 0 is true. q = -0.5 * (1e8 + sqrt_disc) ≈ -100000000.0. c / q = 1.0 / -100000000.0 = -1e-8. The test expects -1e-8 with tolerance 1e-15. But q is computed with rounding error. c/q might be slightly off. The tolerance is extremely tight for a catastrophic cancellation test. Actually CHECK_NEAR(r1, -1e-8, 1e-15, ...) - -1e-8 is the expected value. The absolute difference must be < 1e-15. But the result is around -1e-8. Double precision has ~15-16 decimal digits. The ulp at 1e-8 is about 1e-24. So 1e-15 is actually very loose relative to the value? No, absolute tolerance of 1e-15 on a value of 1e-8 means relative error of 1e-7. That's actually reasonable for a stable formula. But wait, formula_pos is supposed to be the stable one. The Citardauq formula computes c/q where q = -0.5*(b + sign(b)*sqrt(disc)). For b=1e8, q ≈ -1e8. c/q = 1/(-1e8) = -1e-8. The exact value of the root is (-b + sqrt(b^2-4ac))/(2a) = (-1e8 + sqrt(1e16-4))/2. Let's compute: sqrt(1e16-4) = 99999999.99999998 (approx). So -1e8 + 99999999.99999998 = -2.000000000...e-8? Wait. b^2 - 4ac = 1e16 - 4. sqrt(1e16 - 4) = 99999999.99999998? Let's be more precise. (1e8)^2 = 1e16. 1e16 - 4. The square root is slightly less than 1e8. Actually sqrt(1e16 - 4) = 1e8 * sqrt(1 - 4e-16) ≈ 1e8 * (1 - 2e-16) = 1e8 - 2e-8. So -b + sqrt(disc) = -1e8 + (1e8 - 2e-8) = -2e-8. Divided by 2: -1e-8. So the exact root is -1e-8. The Citardauq formula computes 2c / (-b - sqrt(disc)). Wait, the code says:

c

if (b >= 0.0) {
    double q = -0.5 * (b + ml_sqrt(disc));
    return c / q; // Maps to the +sqrt branch when b >= 0
}

For b=1e8, q = -0.5 * (1e8 + ~1e8) = -0.5 * (2e8 - 2e-8) = -1e8 + 1e-8. Wait, sqrt(disc) ≈ 1e8 - 2e-8. So b + sqrt(disc) = 1e8 + 1e8 - 2e-8 = 2e8 - 2e-8. q = -0.5 * (2e8 - 2e-8) = -1e8 + 1e-8. Then c / q = 1.0 / (-1e8 + 1e-8) ≈ -1e-8 * (1 + 1e-16). The result is approximately -1.0000000000000001e-8. The difference from -1e-8 is about 1e-24. So CHECK_NEAR with 1e-15 passes easily. Good.

    test_matrix_invariants(): Inline det3x3, mul3x3, inv3x3 functions. These are in the fuzzer, not in the library. Fine.
    test_quaternion_algebra(): ml_quat q1 = {rand_bounded_double(), ...}; - rand_bounded_double() returns [-10, 10]. ml_quat_mul computes the Hamilton product. The norm check n12 == n1 * n2 uses absolute tolerance ml_fabs(n1 * n2) * 1e-7 + 1e-12. For large quaternions, norms can be up to 400. Product up to 160000. Tolerance is 0.016. That's loose but acceptable for random testing.
    test_complex_identities(): cplx_exponential((cplx){0.0, math_pi}) - CHECK_NEAR(euler.real, -1.0, 1e-9). As noted before, ml_sin(pi) might not be exactly 0. ml_exp(0.0) is 1.0. So real = 1.0 * ml_cos(pi). ml_cos(pi) calls ml_minimax_cos(pi). ml_rem_pio2(pi, &y) gives fn = round(pi * 2/pi) = round(2.0) = 2. r1 = pi - 2*PIO2_HI. PIO2_HI is 1.5707963267948966. 2*PIO2_HI = 3.141592653589793. pi as double is 3.141592653589793115998.... So r1 = pi - 2*PIO2_HI is about 1.2246e-16. r2 = -2 * PIO2_LO = -2 * 6.123e-17 = -1.2246e-16. y = r1 + r2 ≈ 0. So ml_minimax_cos with n=2 returns -ml_minimax_cos_raw(0.0) = -1.0. Exactly. imag = 1.0 * ml_sin(0.0) = 0. So euler.real is exactly -1.0. Good.
    test_v10_tensor_solver(): A_data[i*4+i] = sum + 5.0; - This makes the matrix strictly diagonally dominant, guaranteeing convergence and no zero pivots. It's a weak test. It doesn't test actual pivoting.
    test_simd_and_bare_metal(): __m256d v_in = _mm256_load_pd(in); - in is double in[1024] __attribute__((aligned(32))). But _mm256_load_pd requires 32-byte alignment. The array is aligned, but the loop starts at &in[0], which is aligned. However, the loop for(int i=0; i<1024; i+=4) uses &in[i]. i=0 is aligned. i=4 is in[4], which is 32 bytes from in[0], so also aligned. Good.
    test_fixed_point_cordic(): angle = ml_fmod(angle, 2.0 * 3.14159265358979323846); - ml_fmod for large angles uses long long cast and caps at 9.22e18. If angle is huge (type 2 from rand_double() can be 1e15), angle / (2*pi) is ~1.6e14, well below 9.22e18. So ml_fmod works. But ml_fmod itself has precision issues for large inputs. For angle = 1e15, angle / (2*pi) is about 1.59e14. The cast to long long is exact. The remainder x - qi*y might have catastrophic cancellation because qi*y is huge. But ml_fmod caps the quotient and does a crude remainder. For angles this large, the remainder might have only a few bits of precision. Then the fixed-point CORDIC is compared against double CORDIC with the same imprecise angle. So they agree with each other, but both are wrong. The test passes but proves nothing about accuracy. This is a fundamental flaw in the test design.

v11A3/tests/run_fuzz_vault.py

    repro_code generates a C file that includes ml_core.h, ml_trig.h, ml_exp_log.h, then does srand(seed); and returns 0. It does not actually call any functions or reproduce the failure. The comment says "Add specific failing module calls here if isolated" but the code doesn't do it. This is completely useless as a regression vault.

v11A3/tests/test_dsp.c

    cplx sig[4] = {{1,0}, {0,0}, {0,0}, {0,0}}; - impulse. FFT should give all ones. ASSERT_NEAR(sig[0].real, 1.0, 1e-9, "FFT DC bin"); - Wait, after fft_execute, the DC bin (index 0) should be 1.0. But the Nyquist bin (index 2 for n=4) should also be 1.0? Let's check: DFT of [1,0,0,0] is [1,1,1,1]. The test checks sig[0].real == 1.0 and sig[1].real == 1.0. But it doesn't check sig[2].real or sig[3].real. Actually for n=4, DFT matrix is:
        X[0] = 1+0+0+0 = 1
        X[1] = 1 + 0 + 0 + 0 = 1
        X[2] = 1 + 0 + 0 + 0 = 1
        X[3] = 1 + 0 + 0 + 0 = 1
        So all bins should be 1.0. The test only checks two bins and calls one "Nyquist bin" which is wrong terminology for n=4 (Nyquist is index 2). And it doesn't check the other two. Weak test.

v11A3/tests/run_soak.sh

    Same as soak_test.sh but with slightly different echo text. Duplicate files with minor variations.

v11A3/include/mathlib/ml_core.h

    ml_fp_cast union type-punning: technically undefined behavior in strict ISO C (union of double and uint64_t). In practice, GCC/Clang define it. MSVC does not guarantee it. Better to use memcpy.
    ml_sqrt inline asm: __asm__ ("sqrtsd %1, %0" : "=x" (res) : "x" (x)); - the constraint "x" means any SSE register. But sqrtsd on some assemblers requires the operands to be XMM registers. This is fine for x86_64. However, if x is a memory operand, the compiler might pass it as "m" even though constraint is "x". Actually "x" forces a register. Good.
    ml_fmod: double q = x / y; then if (q > 9.22e18) q = 9.22e18; - why 9.22e18? It's approximately LLONG_MAX as a double. But LLONG_MAX is 9.22e18 exactly? LLONG_MAX = 9223372036854775807 ≈ 9.223372e18. The code uses 9.22e18 which is slightly less, but the cast (long long)q for q = 9.22e18 yields 9220000000000000000 or similar, which is less than LLONG_MAX. However, 9.22e18 as a double literal is exactly 9220000000000000000. So the cap is safe but arbitrary.
    ml_fmod: long long qi = (long long)q; - if x is negative and y is positive, q is negative. Cast to long long truncates toward zero. Then rem = x - qi * y. For x = -5.0, y = 2.0, q = -2.5, qi = -2, rem = -5.0 - (-2)*2.0 = -5.0 + 4.0 = -1.0. Correct. For x = 5.0, y = -2.0, q = -2.5, qi = -2, rem = 5.0 - (-2)*(-2.0) = 5.0 - 4.0 = 1.0. But the mathematical definition of fmod usually has the sign of the dividend. fmod(5.0, -2.0) should be 1.0? Actually C99 fmod(5.0, -2.0) returns 1.0 (sign of x). The code does rem = x - qi * y. With qi truncated toward zero, this is correct.
    ml_fmod: if (ml_fabs(rem) >= ml_fabs(y)) { rem = rem - (rem > 0 ? y : -y); } - if rem is positive and |rem| >= |y|, subtract y. If rem is negative and |rem| >= |y|, subtract -y (i.e., add y). This is correct.
    ml_round: if (x > 9.22e18) return x; - for values above LLONG_MAX, it returns x unchanged. But x might not be an integer. This is wrong. ml_round(9.23e18) should return 9.23e18 (it's already an integer? Not necessarily, but double can't represent fractions at that magnitude). Actually at 9e18, the ulp is 2. So all representable numbers are even integers. So returning x is fine because x is already an integer. But the check x > 9.22e18 is a hack.
    ml_frexp_pure: cast.u += ((uint64_t)exp << 52); - this is wrong for subnormal numbers. If x is subnormal, exp_bits == 0, and the function handles it separately. But for normal numbers, adding exp << 52 to the raw bits is correct only if the exponent doesn't overflow. If exp is large, say 1024, cast.u might overflow into infinity. But ml_ldexp_pure is used internally with n from ml_round, which for ml_exp is bounded. Still, no input validation.
    ml_frexp_pure: *exp = (int)exp_bits - 1022; - standard frexp returns exponent such that x = mantissa * 2^exp, with mantissa in [0.5, 1). The bias is 1023, so exp_bits - 1023 is the true exponent. But this returns exp_bits - 1022. Why? Because the mantissa is set to 0.3FE... which is 0.75, not 0.5? Wait: cast.u = (cast.u & 0x800FFFFFFFFFFFFFULL) | 0x3FE0000000000000ULL; 0x3FE is exponent 1022. So the returned value is in [0.5, 1.0) because the mantissa has the hidden bit set (1.xxx). Wait, 0x3FE0000000000000 is 0.5 exactly. Because the hidden bit is 1, and the mantissa is all zeros, so 1.0 * 2^(1022-1023) = 1.0 * 2^-1 = 0.5. So the returned value is in [0.5, 1.0). Then *exp = exp_bits - 1022. But if the value is 0.5 * 2^exp, then x = 0.5 * 2^(exp_bits - 1022). Wait, x = 1.mantissa * 2^(exp_bits - 1023). If we set 1.mantissa to 1.0 (all zeros), we get 1.0 * 2^(exp_bits - 1023) = 0.5 * 2^(exp_bits - 1022). So yes, *exp = exp_bits - 1022 is correct because the mantissa is now exactly 0.5. But standard frexp returns the mantissa in [0.5, 1) and the exponent such that x = mantissa * 2^exp. So exp = exp_bits - 1023 + 1 = exp_bits - 1022. Correct. But the comment says *exp = (int)exp_bits - 1022; and the example says 8.0 = 0.5 * 2^4. 8.0 has exponent bits 0x402 (1026). 1026 - 1022 = 4. Correct. But standard frexp(8.0) returns 0.5 and exponent 4. So this is correct, though non-standard in that it returns 0.5 instead of [0.5, 1). Actually standard frexp returns [0.5, 1). 0.5 is included. So it's fine.

v11A3/include/mathlib/combinatorics.h

    factorial: if (x < 0 || x > 20) return 18446744073709551615ULL; - UINT64_MAX. But x is int. For x = 21, returns max. For x = 20, returns 2432902008176640000. 21! is 51090942171709440000 which overflows uint64_t. So the cap is correct.
    factorial: uint64_t result = (uint64_t)x; while ((x - 1) > 0) { result *= (x - 1); x -= 1; } - for x=0, the early return handles it. For x=1, result = 1, (1-1) > 0 is false, returns 1. Good. For x=2, result=2, (2-1)>0 true, result*=1, x=1, loop ends. Returns 2. Good.
    npr: if (r < 0 || r > n) return 0; - correct for combinatorics.
    ncr: return num / (den1 * den2); - den1 * den2 can overflow uint64_t even when num / (den1 * den2) would fit. For example, n=60, r=30. den1 = 30!, den2 = 30!. Both are huge. den1 * den2 overflows. But ncr(60,30) is about 1.18e17, which fits in uint64_t. The computation overflows. This is a bug. Should compute as num / den1 / den2 or use cancellation.

v11A3/include/mathlib/simd_bare_metal.h

    ml_avx2_fast_rsqrt: __m256i vi = _mm256_castpd_si256(v); - cast double to int64 vector. Fine.
    __m256i magic = _mm256_set1_epi64x(0x5fe6ec85e7de30daLL); - the magic constant for double rsqrt. This is derived from the 64-bit extension of the Quake III hack. But is it correct? The original Quake III constant 0x5f3759df is for 32-bit float. For double, the constant should be 0x5fe6eb50c7b537a9 according to some sources, or 0x5fe6ec85e7de30da as used here. The exact optimal constant depends on the desired precision. Without derivation comment, we can't verify.
    __m256i vi_half = _mm256_srli_epi64(vi, 1); - logical shift right by 1. For the exponent/mantissa interpretation, this is correct for the hack.
    __m256d sub = _mm256_sub_pd(three_half, _mm256_mul_pd(x_half, y2)); - Newton-Raphson iteration: y = y * (1.5 - 0.5*x*y*y). But the code does y = y * (1.5 - x_half * y * y) where x_half = x * 0.5. That's correct.
    Only one Newton iteration. For float32, one iteration gives ~1e-3 relative error. For double64, one iteration is not enough. The scalar ml_fast_rsqrt does two iterations. The AVX version does only one. This is a precision bug relative to the scalar version.

v11A3/include/mathlib/polynomial.h

    polynomial_newton: if (ml_fabs(dfx) < epsilon) {return 0.0 / 0.0;} - returns NaN if derivative is too small. But the caller might not expect NaN. Should return the current x or a status code.
    polynomial_newton: for (int i = degree - 1; i >= 0; i--) {dfx = dfx * x + fx; fx = fx * x + coeffs[i];} - This is Horner's method for both f(x) and f'(x). But dfx is initialized to 0, and fx to coeffs[degree]. Let's trace: for degree 2, coeffs {c0, c1, c2}.
        i=1: dfx = 0*x + c2 = c2. fx = c2*x + c1.
        i=0: dfx = c2*x + c2*x + c1 = 2*c2*x + c1? Wait, no. dfx = dfx * x + fx. At i=1, dfx = c2, fx = c2*x + c1. At i=0, dfx = c2*x + (c2*x + c1) = 2*c2*x + c1. That's correct for derivative of c2*x^2 + c1*x + c0. And fx = (c2*x + c1)*x + c0 = c2*x^2 + c1*x + c0. Correct. This is the classic Horner dual evaluation.
    Missing include guard for ml_core.h? No, it includes it.

v11A3/include/mathlib/fixed_point.h

    ml_fixed_mul: int64_t res = ((int64_t)a * b) >> 16; - arithmetic right shift of signed integer is implementation-defined in C if negative. Actually for int64_t, right shift of negative values is implementation-defined (arithmetic or logical). Since it's two's complement on all modern platforms, it's arithmetic. But strictly not portable.
    ml_cordic_sincos_fixed: theta = theta % ML_FIXED_TWO_PI; - ML_FIXED_TWO_PI = 411774. For theta near INT32_MIN, theta % 411774 is well-defined in C99 (truncation toward zero). But the range reduction might leave values outside [-π, π] if theta is exactly INT32_MIN and ML_FIXED_TWO_PI doesn't divide evenly. INT32_MIN % 411774 = -2147483648 % 411774. 411774 * 5210 = 2145342540. Remainder is -2140908. Which is less than -ML_FIXED_PI = -205887. So the subsequent if (theta > ML_FIXED_PI) theta -= ML_FIXED_TWO_PI; doesn't trigger (theta is negative). if (theta < -ML_FIXED_PI) theta += ML_FIXED_TWO_PI; triggers: -2140908 + 411774 = -1729134. Still less than -205887. So it needs another subtraction? No, it only does one correction. So after one addition, theta = -1729134, which is still less than -205887? Wait, -1729134 < -205887 is true (more negative). So it should do another addition, but the code only has one if statement, not a while. So the result is still outside [-π, π]. This is a bug. Range reduction for fixed-point CORDIC is incomplete for large negative inputs.
    ml_cordic_sincos_fixed: if (theta > ML_FIXED_HALF_PI) { theta = ML_FIXED_PI - theta; negate_cos = 1; } - for theta = ML_FIXED_PI - 1, theta > ML_FIXED_HALF_PI is true. New theta = 205887 - (205887 - 1) = 1. Good.
    ml_cordic_sincos_fixed: x_new = x - (y >> i); - arithmetic shift of y (signed). For negative y, this is floor division by 2^i? Actually arithmetic right shift rounds toward negative infinity for two's complement? No, it rounds toward negative infinity for positive? Let's think: -3 >> 1 in two's complement is -2 (since -3 is ...1101, shift right arithmetic gives ...1110 = -2). So it rounds toward negative infinity. But CORDIC expects exact division by 2^i. For negative y, y >> i is not the same as y / (1<<i) if we want truncation toward zero. In C, integer division truncates toward zero. (-3) / 2 = -1. (-3) >> 1 = -2. So the shift introduces an error for negative y. This is a bug in the fixed-point CORDIC for negative angles. The correct code should use y / (1 << i) or adjust for rounding.

v11A3/include/mathlib/ml_linalg.h

    ml_lu_decomp_v10: (void)ws; // Suppress unused warning - the workspace is passed but not used because allocations happen in ml_solve_v10. Fine.
    ml_lu_decomp_v10: if (max_val == 0.0) return ML_ERR_SINGULAR; - comparing floating point to exact zero after pivoting. If the matrix is singular but due to rounding the max is 1e-300, this doesn't catch it. Should be if (max_val < ML_EPSILON) return ML_ERR_SINGULAR; where ML_EPSILON is 2.220446049250313e-16 or similar. But for some matrices, small pivots are legitimate (e.g., scaled systems). So max_val == 0.0 is actually the correct check for exact singularity, but it won't catch near-singularity. However, near-singularity is not necessarily an error.
    ml_lu_decomp_v10: double mult = ML_TENSOR_AT(LU, k, i) / pivot; - if pivot is very small, mult is huge. But the code only checks max_val == 0.0. So if pivot is 1e-300, the code proceeds and might overflow. This is a robustness issue.
    ml_solve_v10: double* lu_data = (double*)ml_workspace_alloc(ws, n * n * sizeof(double)); - ml_workspace_alloc aligns to 32 bytes. For n=4, 4*4*8 = 128. Aligned to 32: 128. Fine.
    ml_solve_v10: if (ml_lu_decomp_v10(A, LU, P, ws) != ML_SUCCESS) return ML_ERR_SINGULAR; - ml_lu_decomp_v10 returns 0 on success, but ML_SUCCESS is 0. So this works. But ml_lu_decomp_v10 returns ML_ERR_INVALID_ARG or ML_ERR_SINGULAR on failure. ML_ERR_SINGULAR is -1. The comparison != ML_SUCCESS catches both. Good.
    ml_solve_v10: for (int j = 0; j < i; j++) sum += ML_TENSOR_AT(LU, i, j) * y[j]; - ML_TENSOR_AT(LU, i, j) accesses the lower triangle (L multipliers). Correct.
    ml_solve_v10: x[i] = (y[i] - sum) / ML_TENSOR_AT(LU, i, i); - accesses the diagonal (U pivots). Correct.

v11A3/include/mathlib/ml_complex.h

    cplx_div: if (denom == 0.0) return (cplx){0.0/0.0, 0.0/0.0}; - NaN for division by zero. But denom = b.real*b.real + b.imag*b.imag. If b.real and b.imag are both subnormal, denom might underflow to 0.0 even though b is not exactly zero. Then it returns NaN when it should return a huge number. But practically, if denom underflows, the result is indeed huge and likely inf. Returning NaN is a conservative choice but not mathematically correct.
    cplx_arg: if (a.real == 0 && a.imag > 0) {return math_pi / 2;} - math_pi is not guaranteed to be defined in this header. It relies on ml_trig.h being included first or math_pi being defined elsewhere. But ml_complex.h includes ml_core.h and ml_exp_log.h, not ml_trig.h. So math_pi might be undefined. Wait, ml_exp_log.h doesn't define math_pi. ml_core.h doesn't define it. So if a file includes ml_complex.h without including ml_trig.h, math_pi is undefined. This is a compilation bug. ml_complex.h must define math_pi or include ml_trig.h.

v11A3/include/mathlib/ml_tensor.h

    ml_workspace_alloc: size_t aligned = (bytes + 31) & ~(size_t)31; - correct alignment to 32 bytes.
    ml_workspace_alloc: if (ws->used_bytes + aligned > ws->size_bytes) return NULL; - strict check.
    ml_workspace_alloc: void* ptr = (char*)ws->storage + ws->used_bytes; - ws->storage is void*. Casting void* to char* is fine.
    ml_workspace_t: uint64_t magic_canary; - only in hardened mode. But the struct size changes based on the build profile. This means a library compiled with hardened mode is ABI-incompatible with code compiled without it. If a user includes the header in their project without MATHLIB_PROFILE_HARDENED, but links against a hardened libmathc.a, the struct sizes mismatch. This is an ABI-breaking bug. The canary must be present unconditionally, or the struct must have a fixed size and the canary must be elsewhere.

v11A3/include/mathlib/quaternion.h

    ml_quat_normalize: double inv_mag = ml_rsqrt(mag2); - ml_rsqrt is a macro from profiles.h. If MATHLIB_PROFILE_GRAPHICS is defined, it expands to ml_fast_rsqrt(mag2). If SCIENTIFIC, it expands to (1.0 / ml_sqrt(mag2)). But ml_rsqrt is not defined in ml_core.h or any header included here. It relies on profiles.h being included. It is. But the macro expansion means ml_quat_normalize is not a function call; it's a macro expansion. If ml_rsqrt is used in an expression like ml_rsqrt(mag2) + 1, it's fine. But if someone does &ml_rsqrt, it fails. Not a bug per se, but a design issue.
    ml_quat_slerp: if (dot > 0.9995) { return ml_quat_normalize((ml_quat){...}); } - for very close quaternions, uses linear interpolation. The threshold 0.9995 is arbitrary but standard.
    ml_quat_slerp: double theta = ml_acos(dot); - dot is in [0, 1] after the flip. ml_acos is accurate here. Good.

v11A3/include/mathlib/cpu_dispatch.h

    typedef void (*ml_matmul_func_t)(const double* A, const double* B, double* C, int N); - function pointer type.
    extern ml_cpu_caps_t g_ml_cpu_caps; - global mutable state.
    extern ml_matmul_func_t g_ml_matmul; - global mutable function pointer.
    static inline void ml_matmul(...) { if (!g_ml_cpu_caps.initialized) ml_init_cpu_dispatch(); g_ml_matmul(A, B, C, N); } - race condition. As discussed.

v11A3/include/mathlib/ml_trig.h

    static inline double ml_sin_scientific(double x) { return ml_minimax_sin(x); } - ml_minimax_sin is declared in internal/minimax.h. But in V11S, ml_minimax_sin will be an exported function, not inline. So this inline wrapper is fine.
    ml_sin_fixed: x = ml_fmod(x, 2.0 * 3.14159265358979323846); - ml_fmod for large x is imprecise. Then ml_q16_16_t f_in = (ml_q16_16_t)(x * 65536.0); - if x is near pi, x * 65536.0 is about 205887. Cast to int32_t truncates toward zero. For negative x, this truncates toward zero, not floor. So f_in for x = -0.0001 is -6 (since -0.0001 * 65536 = -6.5536, cast to int is -6). But the CORDIC expects the angle in fixed-point. The truncation is fine but the range reduction ml_fmod is bad.
    ml_tan: if (c == 0.0) return (s > 0) ? 1.0/0.0 : -1.0/0.0; - if s is also 0.0 (e.g., x = pi), returns 1.0/0.0 because s > 0 is false (0.0 > 0 is false), so it returns -1.0/0.0. But tan(pi) should be 0.0/0.0 (NaN) because it's 0/0. The code returns -inf instead of NaN. Bug. Should be if (c == 0.0) return (s == 0.0) ? 0.0/0.0 : ((s > 0) ? 1.0/0.0 : -1.0/0.0);.
    ml_atan: recursive for |x| > 1.0 and |x| > 0.5. For x = 1e300, the recursion depth is small (logarithmic in the exponent). But for x = 1.0 + 1e-16, it hits if (x > 0.5) return (math_pi / 4.0) + ml_atan((x - 1.0) / (x + 1.0));. This is fine. But the Taylor series for (int i = 3; i <= 21; i += 2) computes up to x^21. For x near 0.5, convergence is decent. For x = 0.5, error is small. But the coefficients are not precomputed; it uses term *= -x2 and term / i. The division by i in the loop is an integer-to-double conversion each iteration. Slow but correct.
    ml_asin: return 2.0 * ml_atan(x / (1.0 + ml_sqrt(1.0 - x * x))); - for x near 1.0, 1.0 - x*x is subject to catastrophic cancellation. But ml_sqrt(1.0 - x*x) for x = 1.0 - eps gives sqrt(2*eps - eps^2) ≈ sqrt(2*eps). Then x / (1.0 + sqrt(...)) is about 1 / (1 + sqrt(2*eps)). This is the standard half-angle identity and is stable.
    ml_atan2: if (x == 0.0 && y == 0.0) return 0.0; - C99 atan2(0.0, 0.0) returns 0.0. Good. But atan2(+0.0, -0.0) returns pi in C99. This code returns 0.0 for both +0.0 and -0.0 because x == 0.0 doesn't distinguish signs. Bug: ml_atan2(+0.0, -0.0) should return math_pi, but returns 0.0. Similarly, ml_atan2(-0.0, -0.0) should return -math_pi. The code does not handle signed zeros.

v11A3/include/mathlib/integral.h

    factorial_float: return (ml_sqrt(2 * math_pi * x)) * (ml_pow((x / math_e), x)); - Stirling's approximation. For x = 5, sqrt(10*pi) * (5/e)^5 ≈ 5.604 * 24.69 ≈ 118.3. Actual 120. Error is about 1.4%. The test allows 5 absolute error for factorial_float(5). Fine.
    integral_traditional: if (d == 0.0 || ml_isnan(d) || ml_isnan(a) || ml_isnan(b)) return 0.0/0.0; - NaN for invalid input. Good.
    integral_traditional: if ((d > 0 && a >= b) || (d < 0 && a <= b)) return 0.0; - returns 0.0 for empty interval. Should it return 0.0 or NaN? C99 integral over empty interval is 0.0. Fine.
    gamma_new: if (x > 2) return (x - 1) * gamma_new(x - 1); - recursion depth unbounded. Stack overflow for x = 1e6. Also x is double. If x is not an integer, e.g., x = 2.5, it recurses: 2.5 -> 1.5 * gamma_new(1.5). 1.5 < 2 and > 1, so it goes to polynomial. Fine. But for x = 100000.0, it recurses 99998 times. Stack overflow bug.
    gamma_new: if (x < 1) return gamma_new(x + 1) / x; - for x = 0.5, it calls gamma_new(1.5) / 0.5. gamma_new(1.5) uses polynomial. Fine. But for x = 0.0001, it calls gamma_new(1.0001) / 0.0001. gamma_new(1.0001) uses polynomial. Fine. But for x = -0.5, it calls gamma_new(0.5) / -0.5. gamma_new(0.5) calls gamma_new(1.5) / 0.5. So it terminates. But for x = 0.0, the early return if (x <= 0) return 0.0 / 0.0; catches it. For x = -1.0, returns NaN. Good.

v11A3/include/mathlib/ode.h

    ode_euler: y += dt * f(t, y); - simple Euler. Correct.
    ode_rk4: y += (dt / 6.0) * (k1 + 2.0*k2 + 2.0*k3 + k4); - standard RK4. Correct.

v11A3/include/mathlib/optimization.h

    optimize_golden: double phi = (1.0 + ml_sqrt(5.0)) / 2.0; - golden ratio. Correct.
    optimize_golden: if (f1 < f2) - assumes minimization. If the user wants maximization, they must negate. This is standard.
    optimize_gradient_descent: double grad = derivative(f, x, 1e-5); - uses central difference with step 1e-5. For functions with scale ~1e-10, this is terrible. But it's a generic optimizer; the user is expected to know the scale. The lr (learning rate) is fixed. No line search. Can diverge if lr is too large.

v11A3/include/mathlib/simd_batch.h

    ml_simd_batch_poly: __m256d res = _mm256_add_pd(_mm256_mul_pd(v, v), v); - computes v*v + v. That's x^2 + x. The name says "poly" but it's a specific polynomial. No documentation of what polynomial.
    ml_simd_batch_rsqrt: Two Newton-Raphson iterations. Good for float-level precision in double? Actually each iteration roughly doubles correct bits. Initial guess ~12 bits correct. One iteration ~24 bits. Two iterations ~48 bits. Double needs 53 bits. So two iterations might not be enough for full double precision, but it's within 1e-5 relative. The scalar ml_fast_rsqrt also does two iterations and claims 1e-4 relative error.

v11A3/include/mathlib/statistics.h

    variance: return sum / n; - population variance, not sample variance. This is fine if documented, but usually variance means sample variance (divides by n-1). The function should be named population_variance or documented.
    binomial_pmf: return ncr (n, k) * ml_pow(p, k) * ml_pow(1 - p, n - k); - ncr can return UINT64_MAX on overflow, which when cast to double becomes a huge number. Then ml_pow might overflow. No check.
    normal_pdf: return (1 / ml_sqrt(2 * math_pi * sigma * sigma)) * ml_exp (-z * z / 2); - 1 / ... uses integer division! 1 is int. ml_sqrt(...) returns double. 1 / double promotes 1 to double, so it's 1.0 / .... Actually in C, 1 / double is 1.0 / double because of usual arithmetic conversions. So it's fine. But it looks like integer division. Should be 1.0 / ... for clarity.
    linear_regression: double denom = n * sum_x2 - sum_x * sum_x; - if x values are large, this can catastrophically cancel. For example, x = [1e9, 1e9+1, 1e9+2]. sum_x = 3e9+3, sum_x2 = 1e18 + .... n*sum_x2 and sum_x*sum_x are both ~3e18, and their difference loses precision. This is a known issue with the naive formula. Should use two-pass algorithm or Welford's method. But for a simple library, it's acceptable.

v11A3/include/mathlib/ieee754.h

    logarithm_ieee754: uint64_t mantissa = (cast.u & 0xFFFFFFFFFFFFFULL) | 0x10000000000000ULL; - extracts 52 bits and adds hidden bit. Correct.
    logarithm_ieee754: double m = (double)mantissa / 4503599627370496.0; - 2^52. Correct. m is in [1.0, 2.0).
    logarithm_ieee754: if (m < 0.7071067811865475) { m *= 2.0; e--; } - adjusts to [sqrt(2)/2, sqrt(2)). Correct.
    logarithm_ieee754: double z = (m - 1.0) / (m + 1.0); - series for atanh(z) = 0.5*ln((1+z)/(1-z)). Since m = (1+z)/(1-z), ln(m) = 2*atanh(z). The series computes 2 * (z + z^3/3 + ...). Correct.
    exponential_ieee754: int n = (int)(n_d + (n_d > 0 ? 0.5 : -0.5)); - round to nearest. But for n_d = 0.5, this rounds to 1.0. For n_d = -0.5, rounds to 0.0? Wait: -0.5 + (-0.5 > 0 ? 0.5 : -0.5) = -0.5 - 0.5 = -1.0. Cast to int is -1. But round(-0.5) in C99 rounds away from zero to -1.0. So this matches.
    exponential_ieee754: cast.u += ((uint64_t)n << 52); - adds n to exponent. If n is negative, this subtracts from the exponent. But uint64_t cast of negative n is huge. Bug: If n < 0, ((uint64_t)n << 52) is a huge number because n is first cast to uint64_t (which for negative values becomes 2^64 + n), then shifted. This will corrupt the exponent and sign bits.
    Example: n = -1. (uint64_t)-1 = 0xFFFFFFFFFFFFFFFF. << 52 overflows (UB in C for 64-bit shift? Actually shifting a 64-bit integer by 52 is fine if it's uint64_t). 0xFFFFFFFFFFFFFFFF << 52 = 0xFFFFFFFFF000000000000 truncated to 64 bits = 0xF000000000000. Adding this to cast.u completely destroys the value.
    Fix: cast.u += ((uint64_t)(int64_t)n << 52); No, that doesn't help. The correct way is:
    c

    int64_t exp_bits = (int64_t)((cast.u >> 52) & 0x7FF);
    exp_bits += n;
    cast.u = (cast.u & ~0x7FF0000000000000ULL) | ((uint64_t)exp_bits << 52);

    Or use ml_ldexp_pure which does the same thing. Wait, ml_ldexp_pure has the exact same bug:
    c

    static inline double ml_ldexp_pure(double x, int exp) {
        ml_fp_cast cast; cast.d = x;
        cast.u += ((uint64_t)exp << 52);
        return cast.d;
    }

    This is a critical bug in ml_ldexp_pure and exponential_ieee754 for negative n.

v11A3/include/mathlib/fft.h

    fft_execute: if (!is_power_of_two(n)) return; - silent failure. Should return error code or assert.
    fft_execute: for (int i = 1, j = 0; i < n; i++) { ... } - bit-reversal. The inner loop for (; j & bit; bit >>= 1) {j ^= bit;} is standard. But j ^= bit; then j ^= bit; outside? Let's trace: for i=1, bit = n>>1. j & bit is 0 & bit = 0, so skip loop. j ^= bit sets j = bit. If i < j, swap. For i=1, j = n/2. If n>2, swap. This is correct.
    fft_execute: if ((j & 63) == 0) { ... } - periodic twiddle refresh. j is the butterfly index within the sub-block. The condition (j & 63) == 0 means every 64 butterflies, recompute w from scratch. This is to prevent phase drift from accumulated multiplication errors. But j resets to 0 for each len block. So within each block of size len, every 64 butterflies it refreshes. This is correct.
    fft_execute: w = cplx_mul (w, wlen); - after the refresh, it still does w = cplx_mul(w, wlen) at the end of the loop. So after refresh, w is set to cos(theta), sin(theta), then multiplied by wlen for the next iteration. The next iteration starts with w = wlen * cos(theta) + ... which is cos(theta + ang). Correct.
    ifft_execute: for (int i = 0; i < n; i++) {x [i].imag = -x [i].imag;} - conjugate. Then fft_execute. Then conjugate and divide by n. Correct.

v11A3/include/mathlib/ml_types.h

    Fine.

v11A3/include/mathlib/quadratics.h

    ml_formula_pos: if (b >= 0.0) { double q = -0.5 * (b + ml_sqrt(disc)); return c / q; } - This is the Citardauq formula. For b >= 0, it computes q = -0.5*(b + sqrt(disc)), then returns c/q. The standard stable formula is:
        q = -0.5 * (b + sign(b)*sqrt(disc))
        x1 = q / a
        x2 = c / q
        Here, for b >= 0, q = -0.5*(b + sqrt(disc)). Then c/q is the "other" root (the one that avoids cancellation). But the function is named formula_pos suggesting it returns the root with +sqrt. The comment says "Maps to the +sqrt branch when b >= 0". Let's verify: for b >= 0, sqrt(disc) >= 0. q = -0.5*(b + sqrt(disc)) is negative. c/q is negative if c is positive. The +sqrt root is (-b + sqrt(disc))/(2a). If b > 0 and

System is currently busy. Please try again later.
Capacity is busy. Please wait or
upgrade


K2.6

