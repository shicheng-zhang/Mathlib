# MathLib v11S: The Math Diary Protocol

This directory is the **Orchestrator's Interactive Textbook**.

Because the v11A3 codebase was heavily generated by the Blue Team (AI), the mathematical foundations are currently "black boxes." To transition from *Vibe Coder* to *Systems Architect*, you must reverse-engineer these black boxes.

### How to use this directory:
1. Open a `.md` file (e.g., `01_trig_minimax.md`).
2. Look at the corresponding C source code in `src/` or `include/mathlib/internal/`.
3. Take the specific questions from the **"Dark Arts"** and **"Error Budget"** sections to the **Red Team** (Gemini/ChatGPT) or **Green Team** (Copilot).
   - *Prompt Example:* "Red Team, look at `src/exp_log.c`. Explain to me like I'm a computer science student why we use Cody-Waite splitting for `ml_log` instead of standard floating-point modulo. What happens to the bits if we don't?"
4. Record their answers in the **Orchestrator's Reverse-Engineering Log** at the bottom of the file.

Once a file is fully filled out and verified by the Red Team, that module is considered **Mathematically Owned** and ready for the v11S stable release.
